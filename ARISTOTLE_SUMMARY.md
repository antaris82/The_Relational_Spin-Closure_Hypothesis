# Summary of changes for run bd1e246e-52b2-4b73-989a-5062ae14d3a4
TASK 42 — Warning-Zero Release Cleanup: PASS.

**Baseline.** A full `lake build RequestProject` of the frozen release was captured in `task42_before.log`: `Build completed successfully (8356 jobs)` with exactly 76 `warning:` lines, distributed exactly as expected (Clifford 41, PauliRepresentation 13, MinkowskiMatrix 10, AlexanderWhitney 7, Shell 2, MatrixModel 1, EmergentSpinGate 1, CocycleGaugeOrbit 1) across the four categories (69 unused simp arguments, 3 unnecessary sequential focus, 2 deprecated `Matrix.mul_eq_one_comm`, 2 unused section variables).

**Repairs, all at source.** 69 unused entries were deleted from the existing `simp` / `simp only` argument lists — no list was broadened, no proof replaced. Two special cases: where every argument of a `simp [...]` was flagged, the now-empty bracket list was dropped (the tactic was already plain `simp`); where the sole argument of a `simp only [Fin.val_mk]` was flagged, the step's definitional normalisation is retained as `dsimp only`, because deleting it made the following `omega` fail. The three `<;>` occurrences the linter proved superfluous were replaced by ordinary sequencing. Both deprecated uses were switched to the current `mul_eq_one_comm`, with no alias left behind. The two unused section variables were removed with `omit [IsTopologicalGroup L] in` on the two compiler-identified theorems. No linter was disabled, no log filtered, no build command altered; eight Lean files changed, 41 insertions / 40 deletions, all inside tactic blocks apart from the two `omit` lines.

**Final verification.** `task42_after.log`: 8356 jobs, 0 errors, 0 warnings (the word "warning" does not occur in the log at all). A clean rebuild after deleting the project build artefacts (`task42_clean.log`) likewise gives 8356 jobs and 0 warnings. Compile-time firewalls report `SPINE FIREWALL AUDIT: all checks passed` and `SPINE ARCHITECTURE AUDIT: all checks passed` (all per-task firewall lines, illegal edges 0). The regenerated axiom audit is byte-identical to the pre-cleanup file (67 endpoints, only `propext, Classical.choice, Quot.sound`), so the axiom boundary is unchanged. `audit/verify_documentation_snapshot.py` → all checks passed, hash mismatches 0; `audit/test_statement_extraction.py` → all checks passed, 67 declarations, 0 extraction problems. Registries were regenerated and came out byte-identical (no `statement_text` or claim mapping changed, since the `omit` lines precede the declaration headers); only the recorded source/release manifest hashes moved. No `sorry`, `admit`, `axiom`, `native_decide`, `unsafe`, `partial` or `implemented_by` was introduced.

**New artefacts.** `audit/check_zero_warnings.py` — a trivial guard that reads a captured build log and exits nonzero if any line begins with `warning:` (verified in both directions: PASS/warnings=0 on the after-log, FAIL/warnings=76 on the before-log); `TASK42_WARNING_ZERO_AUDIT.md` with the 76→0 table by module and category, a per-file record of every edit with the statement/assumptions/conclusion/strategy impact questions (including the explicit note that the two repairs remove an unused `[IsTopologicalGroup L]` instance parameter — fewer assumptions, unchanged conclusion), the regression-check table and the historical record that the Task-41 release built with 76 warnings while Task 42 builds with 0; `TASK42_FAILBUILDS.md` with the three failures encountered and their repairs. One incidental finding is recorded as OBS-42-001 (out of scope, not modified): the legacy import-audit script reports a pre-existing FAIL identically on the pre-cleanup tree; it emits no build warning and is superseded by the compile-time firewalls. No other source cleanup was performed and no mathematics was opened. All work is committed.

# Summary of changes for run 2cb4266e-758a-4157-997e-f44b09840f9e
Task 41 (release-snapshot repair) is complete: all four defects are fixed, the frozen snapshot is regenerated and validated, and the mathematics is untouched — `git status` shows **no modified file under `RequestProject/`**, so no theorem statement, hypothesis or construction of Tasks 36–40 changed, and no new research branch was opened.

**ISSUE-41-01 (smooth-bundle causal explanation).** Read the source first (`RequestProject/Spine/Solder/BundleEquivalence.lean` correction notice and `RequestProject/Spine/Task36/SmoothBundlePackaging.lean`) and repaired every current-release occurrence of the obsolete claim that strong smooth Lorentz bundle equivalence is blocked because the native Spin cocycle is only continuous: `docs/mathematics/GLOBAL_MATHEMATICS.md`, `docs/paper/PAPER_I_FACT_SHEET.md`, `docs/paper/PAPER_I_DO_NOT_CLAIM.md` (entry 12 rewritten, new 12b), `docs/machine/CLAIM_REGISTRY.jsonl` (CLAIM-G008), `docs/machine/THEOREM_REGISTRY.jsonl` (THM-G008 guard, via its generator), `INTERMEDIATE_MILESTONE_README.md`, `PROJECT_INTERMEDIATE_HANDOFF.md`, `README.md`, and `TASK39_AUDIT.md` (historical verdict preserved, with an explicit Task-41 correction note). The release now states the certified distinction — smooth local/projected Lorentz transition representatives from a regular smooth solder; infrastructure/packaging limitation for the strong total-space package — plus the guard that smooth projected Lorentz data is not a packaged smooth intrinsic Spin Lie-group bundle theory.

**ISSUE-41-02 (`supporting_theorem_ids`).** CLAIM-S010 now lists only theorems (THM-S019, THM-S020, THM-S021); the two definitions `loopSolderTransport` and `loopSolderEquiv` moved to a single new optional, backwards-safe field `supporting_construction_ids`. The validator now rejects any definition in `supporting_theorem_ids` for every claim (confirmed to fire on the pre-repair registry); no registry was renamed and no unrelated claim migrated.

**ISSUE-41-03 (statement extraction).** `audit/lean_statement.py` no longer treats a `:=` belonging to a term-level binder (`have`/`haveI`/`let`/`letI`/`suffices`) inside a result type as the body delimiter, using structural scanning only (no Lean parser, no new dependency). All 67 `statement_text` entries were regenerated — THM-G004 was not patched by hand and is the only entry whose text changed; it now runs to `… = B.tangentTransitionMap i j y`. New `audit/test_statement_extraction.py` carries the mandatory regression case (it fails on the old output), four synthetic fixtures and re-extraction plus conservative sanity checks of every registered declaration; each entry carries an explicit `statement_extraction_status` (currently 67 OK, 0 UNRELIABLE).

**ISSUE-41-04 (selected `cle 0` control direction).** The Task-37/38 family is now classified as a deliberately selected one-parameter subgroup generated by the chosen Clifford direction `cle 0`: `selection_status: SELECTED_CONTROL` / `exhaustive: false` / `selection_note` on OBJ-031/032/033, `canonical_status: SELECTED_CONTROL` / `exhaustive: false` on CLAIM-L010, the family moved to `control_specific_data` in `PROJECT_STATE.json`, and the wording added to `LOCAL_MATHEMATICS.md`, the fact sheet, the do-not-claim list (new guards 19b/19c), the milestone README and the handoff. No second deformation direction was added.

**Regeneration and validation.** Registries, DAGs, project index, failbuild ledger and manifests regenerated; a Task-41 provenance event appended; `docs/machine/REGISTRY_SCHEMA.md` documents the new fields and sentinels. Final results: documentation validator PASS (hashed files 381, hash mismatches 0), statement-extraction regression PASS, `lake build RequestProject` green with the same 8356 jobs, axiom audit regenerated from the build log and byte-identical (67 endpoints, PASS), and all architecture/firewall audits pass. New records `TASK41_RELEASE_SNAPSHOT_REPAIR_AUDIT.md` (four principal sections with defect, files, repair, source used, the four impact questions and final validation) and `TASK41_FAILBUILDS.md` (five recorded failures, kept after the final PASS, plus three out-of-scope observations OBS-41-001…003). Final label: TASK 41 — RELEASE SNAPSHOT REPAIR: PASS / FINAL FROZEN.

# Summary of changes for run b474fd29-234f-48dd-a6b2-c9437f156f9e
Task 40 (intermediate release hardening) is complete; the mathematics of Tasks 36–39 is unchanged and the release validates end to end.

**Frozen baseline.** `lake build RequestProject` is green with 8356 jobs before and after; the only change under `RequestProject/` is five audit-only `#print axioms` lines in `RequestProject/Spine/Closure/AxiomAudit.lean` (that module contains no declaration, so no theorem statement, hypothesis or construction was touched). All firewalls print their pass lines (Spine legacy/layering, architecture DAG, Task-36 battery, deformation/closure leaf and token audits), and the axiom audit now covers 67 endpoints, all reporting `[propext, Classical.choice, Quot.sound]`.

**The six review defects, all fixed** (table with evidence in `TASK40_RELEASE_HARDENING_AUDIT.md` §2): (1) the Task-38 result is rescoped everywhere to the specific Task-37 family on the frozen periodic fixed-cover control, with the Task-39 strengthening stated and an explicit "GENERAL ČECH-DEFORMATION CLASSIFICATION: NOT PROVED" guard; (2) every false "no Lie algebra" statement is removed — the source was read, and `SpinCore.gB`, `SpinCore.gBLie`, `SpinCore.bivectorEquivSkew`, `finrank_gB`, `gBLie_bracket`, `gB_bracket_mem`, `bivectorEquivSkew_wedge` are now registered as OBJ-043…OBJ-046 and THM-L012…THM-L015 under the new CLAIM-L011; (3) the type-incorrect `spinCover ∘ ω` wording is withdrawn and replaced by the requirement that a future Lorentz connection be induced through `dρ_e` or a proved equivalent infinitesimal representation (CLAIM-B011); (4) `docs/machine/PROJECT_STATE.json` now uses the strict six-key taxonomy (primitive local input = ℝ³ with `sip` only; global data, gate data, conditional derived outputs and control data separated); (5) definitions are no longer presented as theorems — every registry entry carries `declaration_kind` read from source, the two definitions get `proof_status: CONSTRUCTED`, and `loopSolderTransport_involutive` was registered as THM-S021 so CLAIM-S010 has real theorem support; (6) the Task-39 module count is corrected to four modules / 794 lines, computed from the tree. Five further defects found during the work (missing Task-39 failbuild records, the handoff outside validation, the hand-written axiom audit, unchecked manifest hashes, mis-typed global data) are listed openly in §3.

**Deliverables.** New `NEXT_PHASE_TRANSPORT_GATE.md` (canonical and validated) replaces the draft handoff, which is retained as a withdrawal stub quoting its three invalid statements; new `TASK40_RELEASE_HARDENING_AUDIT.md`, `TASK40_FAILBUILDS.md` (six recorded validator/registry/manifest failures plus the recorded source change, each with command, file, error, diagnosis, repair and the four impact answers) and `docs/machine/REGISTRY_SCHEMA.md` documenting every enumeration. Updated: `INTERMEDIATE_MILESTONE_README.md`, `PROJECT_INTERMEDIATE_HANDOFF.md`, `ARCHITECTURE.md`, `PROVENANCE_LEDGER.md`, `TASK39_AUDIT.md`, `docs/mathematics/LOCAL_MATHEMATICS.md` (new §6 on the Lie-algebra layer and a corrected regularity boundary), `docs/paper/PAPER_I_FACT_SHEET.md` and `PAPER_I_DO_NOT_CLAIM.md` (new guards §16–§19). The validator `audit/verify_documentation_snapshot.py` was extended with declaration-kind enforcement, proved-claim support rules, classification-consistency checks, overclaim sentinels (with a quoted-invalid context rule) and full manifest hash verification; a new `audit/build_axiom_audit.py` derives the axiom audit from a real build log.

**Final state.** The regenerated snapshot reports: claims 67, registered declarations 67 (65 theorems, 2 definitions), objects 46, modules 58, controls 11, open problems 12 (new OP-012 replaces "construct a Lie algebra" with the infinitesimal-bridge problem), failbuild records 174, provenance events 17, 8 canonical documents, 379 hashed files, 0 hash mismatches — "DOCUMENTATION SNAPSHOT: all checks passed". The mathematical DAG shows the certified bivector/`gB` branch as ordinary edges with `status: CERTIFIED` and keeps the missing `Lie(SpinGroup) ≃ gB`, `dρ_e` and connection bridges in a separate `open_edges` field, never as dependencies. Label: TASK 40 — INTERMEDIATE RELEASE HARDENING: PASS / FROZEN. All work is committed.

# Summary of changes for run c0ddfcab-d513-475d-8dca-fcc93fd17798
Task 39 (intermediate project closure) is complete and the repository is frozen as a reviewable intermediate release.

Mathematics (all new Lean modules build green and are sorry-free):
- `RequestProject/Spine/Deformation/SmoothProjectedGauge.lean` (§4): the projected Lorentz gauge used by the Task-38 smoke test is identified as the native projection of the Spin-side gauge (`loopProjectedGauge_eq`) and proved C^∞ on every chart domain (`contDiffOn_loopProjectedGauge`), together with its cocycle law. The Spin-side gauge is still only continuous, and the documentation says so explicitly.
- `RequestProject/Spine/Deformation/SolderTransport.lean` (§5): the optional strengthening turned out to be short and assumption-neutral, so it was proved: an explicit involutive transport of regular solder solutions, a bijection of the regular solder solution spaces at two parameter values (`loopSolderEquiv`), invariance of the induced tangent Lorentz metric (`tangentMetric_loopSolderTransport`), and the combined `loopSolderSolutionSpace_correspondence` — all scoped to the frozen periodic fixed-cover control base.
- `RequestProject/Spine/Closure/IntermediateMilestone.lean` (§7): the aggregate milestone certificate, containing only already certified conjuncts; solder-dependent conjuncts are implications, so no existence of a regular solder is asserted.
- `RequestProject/Spine/Closure/AxiomAudit.lean` (§36): `#print axioms` on 62 principal endpoints; all report `[propext, Classical.choice, Quot.sound]`.
- `RequestProject/Spine/Deformation/Firewall.lean` was extended audit-only (no check relaxed) with a new leaf check for the closure branch. No frozen Task-36/37/38 statement, hypothesis or definition was modified.

Documentation and snapshot: `INTERMEDIATE_MILESTONE_README.md`, `PROJECT_INTERMEDIATE_HANDOFF.md`, `TASK39_AUDIT.md`, `TASK39_AXIOM_AUDIT.md`, `TASK39_CODE_HYGIENE.md`, `TASK39_FAILBUILDS.md`, `TASK39_PROVENANCE.md`; `docs/mathematics/{LOCAL_MATHEMATICS,GLOBAL_MATHEMATICS,LOCAL_GLOBAL_INTERFACE}.md`; `docs/paper/{PAPER_I_FACT_SHEET,PAPER_I_DO_NOT_CLAIM}.md`; machine-readable registries in `docs/machine/` (project state, 62 theorems, 64 claims, 42 objects, 58 modules, 11 negative controls, 11 open problems, 162 mirrored failbuild records, provenance ledger, axiom audit, three separate DAGs plus `docs/graphs/*.dot`, source and release manifests, generated `PROJECT_INDEX.tsv`), with generators and the validator under `audit/`. Historical task documents and failbuild ledgers were preserved unchanged.

Claim discipline: the Task-38 result is stated everywhere in its scoped form (the tested one-parameter native Spin Čech-transition family on the frozen periodic control is globally admissible for all finite parameter values, lies in one full-Spin Čech gauge orbit, and on that control the regular solder solution spaces correspond with an unchanged tangent Lorentz metric); the universal reading "transition-cocycle deformations are pure gauge" is explicitly forbidden, as are curvature/connection/holonomy and characteristic-class claims that the source does not prove.

Verification: `lake build RequestProject` completes successfully with 8356 jobs (the documented job count was corrected from 8355 to the reproducible 8356 after two identical runs), the architecture, Task-36 and deformation/closure firewalls print their pass lines, the axiom audit is clean, and `python3 audit/verify_documentation_snapshot.py` reports "DOCUMENTATION SNAPSHOT: all checks passed". No `sorry`, `admit`, new `axiom`, `native_decide`, `unsafe`, `partial`, `implemented_by` or `Matrix.inv` occurs in the new modules, and they use no `simp`/`simpa`/`Classical`; the twelve pre-existing localized `open Classical in` sites and the two meta-level `partial def` audit helpers are listed exactly. All work is committed.

# Summary of changes for run 3db96509-1e77-4c3f-8488-1d244bcf937e
Task 38 is complete and the project builds green (`lake build RequestProject`, 8 352 jobs), with no `sorry`/`admit`/new `axiom`/`native_decide`/`unsafe`/`partial`/`implemented_by`/`Matrix.inv` in any new module and every new endpoint reporting `[propext, Classical.choice, Quot.sound]`.

PART I — the Task-37 input was frozen: the full build was re-run before any edit, no existing module was modified, and `transportState`, `loopSharedSpin`, `loopTransportFamily`, `RegularClosureSolution`/`RegularClosureAdmissible` and the exact λ = 0 regression are used verbatim. `ControlAAdmissible` is literally the Task-37 predicate applied to the fixed-base family; it was not weakened and no compatibility field was added. A theorem-level interface certificate records what is varied (native Spin transition/cocycle state), what is derived (the Lorentz transition, only through the native cover), what is held fixed (the base gluing — proved, not assumed) and what is solved for (a regular tangent solder); transport one-forms, path-ordered exponentials, loop-transport functionals, field strengths, Riemann tensors and field equations are absent and are named as absent.

PARTS II–V — results. A closed form for the projected shared transport state was computed inside the project's own Clifford algebra and identified with the native projection, giving a parameter-smooth family of automorphisms of the local model. With it, the admissibility locus of the fixed control is solved exactly for arbitrary λ, not sampled: **every** finite λ is admissible (`R_A = ℝ`), the witness being an explicitly constructed compensating regular solder — the identity on the first patch of each loop and the projected transport state at an interpolated parameter on the second — with smoothness, fibrewise invertibility and the exact intertwining equation proved on every overlap component in both crossing directions. λ ↔ −λ symmetry follows. The critical gauge test then shows that the same interpolation profile exhibits the deformed Spin cocycle as a coboundary: all λ lie in a single orbit of a full-group cocycle gauge equivalence whose convention is derived from the project's existing kernel-valued notion (continuity is part of the notion, and continuity of the shared state in its parameter was proved for it); pushing the same Spin gauge through the native projection gives the Lorentz-side statement, recorded separately. At the narrower kernel-valued equivalence a nonzero λ is *not* equivalent to 0, which only records that the projection moves. Finally, the induced tangent Lorentz metrics of the explicit witnesses are **literally equal** for all λ, because the witnesses differ chartwise by an element of the intrinsic Lorentz group. A first-order control shows the family is nevertheless not parameterwise constant. All of this is bundled in `native_transport_knob_smoketest_certificate`, which contains only proved statements.

Scientific outcome: **C / PASS-C0 — universally admissible and pure gauge**, a successful negative smoke test: on this fixed control base the native Čech transition-cocycle knob is trivialisation freedom, not a closure-sensitive geometric degree of freedom. The anti-vacuity audit classifies the universal theorem as class A (a genuine compensating solder) plus B (coboundary direction) and rejects class C item by item — the tangent-space/local-model identity is not used, the Lorentz side carries no independent knob, the base is frozen, compatibility is never a supplied field, and the solder is not the identity for λ ≠ 0; the Task-37 controls showing that the closure predicate can fail everywhere or hold exactly at 0 still hold, so the result is a property of this knob, not of the interface. No co-varying base was introduced and no target geometry appears anywhere; the mechanical branch firewall was extended to all nine modules (no check relaxed) and passes.

Deliverables: five new Lean modules inside the existing deformation leaf branch (`ProjectedParaAction`, `FixedBaseSmokeTest`, `CocycleGaugeOrbit`, `TangentMetricComparison`, `SmokeTestCertificate`), the firewall extension, and the documents `TASK38_PROVENANCE.md`, `TASK38_AUDIT.md`, `TASK38_CODE_HYGIENE.md`, `TASK38_FAILBUILDS.md` (eleven recorded failures with command, location, exact error, diagnosis, repair and the three impact answers — no statement, assumption or architecture was changed by any repair), `TASK38_SMOKETEST_RESULT.md` (the full result matrix and the per-quantity λ-dependence table), `TASK38_GAUGE_CLASSIFICATION.md`, and — as Outcome C requires — `TASK39_CONNECTION_TRANSPORT_GATE_HANDOFF.md` specifying the smallest next transport layer without implementing it, together with updates to `DEPENDENCY_DAG.md`, `ARCHITECTURE.md` and `PROVENANCE_LEDGER.md`. `ARISTOTLE_SUMMARY.md` was read but not edited. All work is committed.

# Summary of changes for run 094ce085-60f4-48a6-9116-69d635bb7665
Task 37 is complete and the project builds green: `lake build RequestProject` finishes with 8 347 jobs, the Spine architecture audit, the Task-36 firewall and the new Task-37 deformation firewall all print their pass lines, there is no `sorry`/`admit`/new `axiom`/`native_decide`/`unsafe`/`partial`/`implemented_by`/`Matrix.inv` anywhere in the new or edited modules, and every new endpoint reports `[propext, Classical.choice, Quot.sound]`.

PART I — Task 36 frozen. The baseline state (build green, 8 341 jobs; 53 Task-36 axiom audits) was recorded before any edit. Eleven import lists were rewritten so the general layers no longer depend on the adversarial loop models: `OrientationGate` and `TimeOrientation` are now loop-free, `SmoothBundlePackaging` and `GaugeGroup` dropped unused dependencies, `SpinObstruction` no longer pulls the one-chart control, and the depth from the frozen solder layer to the Task-36 firewall fell from 9 to 5, with no umbrella module and no production use of `Certificate`. The loop-specific time-orientation control moved verbatim into `RequestProject/Spine/Task36/LoopTimeOrientation.lean`. The arbitrary-basis determinant-continuity lemma (which needed `classical`) was replaced by `Task36.continuous_det_localModel`, built from the project's own explicit `LocalModel ≃ₗ (Fin 4 → ℝ)` equivalence — shorter, and the `classical` is gone. The remaining localized `Classical` use (the piecewise sign function on the wrap component) was audited, deliberately retained, documented exactly, and the earlier "0 Classical" hygiene claim was corrected. Loop-model wording was corrected everywhere to the exact proved terminology (periodic fixed-cover loop model, two-component periodic overlap, project-native loop-sign control, fixed-cover ℤ₂ lift freedom); no `S¹ × ℝ³` homeomorphism and no π₁ statement is claimed. All Task-36 endpoints — classical reconvergence, the orientation-reversing negative control, both contrapositives, the one-loop/fixed-cover ℤ₂ freedom, the N = 2 four-class family, the free and transitive regular gauge action, and the bundled adversarial certificate — survive unchanged.

PART II — deformation preparation (experiment NOT run). A new leaf branch `RequestProject/Spine/Deformation/` (five modules, ~1 200 lines) contains: an exact source-level audit of what "transport" means in this project, with connection, parallel transport and holonomy marked NOT FORMALIZED; one shared native Spin-side transport state `transportState : ℝ → SpinGroup` built inside the project's own Clifford algebra, with `transportState 0 = 1` exactly, the one-parameter group law, injectivity, and a derived Lorentz projection that is provably *not* the identity for λ ≠ 0 (so the knob is visible to the solder square, unlike the ±1 kernel twist); a λ-deformed native Spin transition datum on the frozen periodic fixed-cover loop model with its Čech cocycle law proved, whose λ = 0 member *equals* the frozen trivial seed and is solved by the frozen regular solder; a two-field family interface (base + Spin only, no Lorentz field) that is parameter-type generic so a later λ(x) sector needs no redesign; the closure objects `RegularClosureSolution` / `RegularClosureAdmissible` with closure existence as the first observable; the CONTROL A (fixed base) versus MAIN TEST B (co-varying base) distinction; and a regime vocabulary for λ = 0, finite λ ≠ 0 and the λ → 0±, λ → ±∞ limits, with no regime decided. Non-vacuity is proved, not asserted: a family that closes at no parameter value, a co-varying family that closes exactly at λ = 0, and hence no universal existence theorem. A compile-time firewall enforces that the branch is a leaf, that the family and solution structures have exactly their two allowed fields (no independent Lorentz knob, no supplied compatibility), and that no declaration names curvature, κ, a cosmological constant, a sphere, hyperbolic space, de Sitter, anti-de Sitter, Einstein equations, a Riemann tensor, a connection or a holonomy.

Stop condition reached: PASS-A, with the honest PASS-B clause that the deformation is prepared at the level of transition cocycles because that is the strongest transport object the project actually has. Documentation: `TASK37_PROVENANCE.md`, `TASK37_AUDIT.md`, `TASK37_CODE_HYGIENE.md`, `TASK37_FAILBUILDS.md` (six failures plus the one recorded architecture change), `TASK37_DEFORMATION_INTERFACE.md`, `TASK38_SHARED_TRANSPORT_SMOKETEST_HANDOFF.md`, plus updates to `DEPENDENCY_DAG.md`, `ARCHITECTURE.md`, `PROVENANCE_LEDGER.md`, `README.md` and correction notices appended (never deleting historical text) to `TASK36_CODE_HYGIENE.md` and `TASK36_AUDIT.md`. The Properties table lists the new Task-37 deliverables with their verified status.

# Summary of changes for run 180a5520-0536-4f29-804d-dd72b368b011
Task 35 is complete: the project builds green (`lake build RequestProject`, 8327 jobs, architecture audit passing), with no `sorry`, `admit`, new `axiom`, `native_decide`, `unsafe`, `partial`, `implemented_by` or `Matrix.inv` in any new module, and every new endpoint audited in-source to `[propext, Classical.choice, Quot.sound]`.

Mathematical outcome — the decisive question is answered YES, as a theorem. `SpinNative.smooth_solder_iff_regular_bundle_equivalence` (`RequestProject/Spine/Solder/BundleEquivalence.lean`) proves that a regular solder datum exists **iff** there is a bundle-level equivalence of the internal Lorentz bundle with the genuine tangent bundle of the Task-33 emergent manifold — a fibrewise continuous-linear equivalence whose total-space maps are continuous both ways (a homeomorphism covering the identity), with `C^∞` local representatives obeying the exact intertwining square. So no further geometric primitive is hidden between "regular solder" and "bundle equivalence". Two residual items are named exactly and not over-claimed: a *smooth* total-space diffeomorphism is not available because the native Spin transition datum carries only continuity (and the pinned library has no smooth-bundle-equivalence type), and a *global* smooth timelike field remains an additional theorem.

Work delivered in this session's final stage: (a) proof-hygiene pass — the last `simp` tactic call in the new production modules was replaced by a structural term, and four Task-34 production-layer `simp`/`simpa` calls in `Solder/Solder.lean` and `Solder/Independence.lean` were replaced by explicit structural proofs (all re-verified by build); (b) full documentation set — `TASK35_PROVENANCE.md`, `TASK35_AUDIT.md` (the pinned-library API audit: what exists, what is absent, what was therefore built natively), `TASK35_CODE_HYGIENE.md` (every remaining automation use itemised with justification), `TASK35_FAILBUILDS.md` (twelve failed builds with command, location, error, diagnosis, repair; none changed a statement, assumption or the architecture), plus updates to `DEPENDENCY_DAG.md`, `ARCHITECTURE.md`, `PROVENANCE_LEDGER.md` and `README.md`; (c) correction notices appended (never deleting historical text) to `TASK34_PROVENANCE.md`, `TASK34_AUDIT.md`, `TASK34_DOCUMENTATION_REPAIR.md`, `TASK34_CODE_HYGIENE.md` and to the module docstrings of `Solder/Independence.lean` and `Solder/Solder.lean`, restating the Task-34 result exactly as "direct/raw transition identification is not forced" and "the fibrewise/algebraic gate closes; the topological/smooth gate remained open".

Earlier stages (already in the repository) cover: the dependency-DAG repair with mechanical firewall enforcement; the regular solder structure local on the cover with `C^∞` comparison fields; the negative control showing a weak Task-34 solder need not be regular; the smooth Lorentz metric (symmetry, nondegeneracy, signature (1,3), tensor law, `C^∞` coefficients); orientation/time-orientation reductions with a chartwise `C^∞` timelike field and the exact gluing criterion; the regular gauge torsor (transitivity and freeness); the Spin endpoint classified as a topological Spin lift with the missing datum named; positive controls on the symmetric and rescaling gluings; and the principal certificate `SpinNative.SmoothTangentSolderData.smooth_solder_tangent_certificate`, which contains no Stiefel–Whitney class, connection, curvature, torsion, field-equation or quantization statement. The Properties table has been reconciled with the final state of the project.

# Summary of changes for run 372a3898-7b87-4e57-ad3c-a4e56635d068
**Task 34 — Tangent/Solder coupling gate: closed, with outcome B.**  Everything below is machine-checked: `rm -rf .lake/build && lake build RequestProject` is green (8319 jobs, zero errors), no `sorry`/`admit`/new `axiom`/`native_decide`/`unsafe`/`partial`/`implemented_by` in any new module, and `#print axioms` on every principal new endpoint reports exactly `propext, Classical.choice, Quot.sound`.

**New mathematics (six new modules).**
1. `RequestProject/Spine/Emergent/TangentTransition.lean` — the genuine tangent transitions of the Task-33 emergent smooth 4-manifold. `tangentTransitionMap = D(φ_ij)`, packaged as a continuous linear equivalence whose invertibility is *derived* from the primitive's inversion law by the chain rule, and the principal theorem `tangentTransition_eq_derivative_baseTransition`: for two charts of the *actual* Tasks-32/33 atlas, Mathlib's own `tangentBundleCore` coordinate change equals `D(φ_ij)`. The definitional identity `TangentSpace localModelI x = LocalModel` is isolated and labelled an implementation artefact; it is never used as a coupling proof.
2. `RequestProject/Spine/Solder/InternalLorentz.lean` — the internal Lorentz transition `ρ(g̃_ij)` of an emergent Spin seed acting on the local model through the native `GLor` (no external SO(1,3)), with inherited preservation of `NS`, `BS`, the cone, the interior future, `det = 1`, the Čech laws and continuity.
3. `RequestProject/Spine/Solder/Independence.lean` — the **main scientific answer**: an explicit smooth two-chart base gluing with `φ₀₁(v) = 2·v`, paired with the pointwise trivial Spin datum, has identity internal Lorentz transition but tangent transition `2·id`. So a smooth base gluing plus a coherent Spin-native gluing does **not** force the two transition systems to agree. This is non-derivability, not non-existence.
4. `RequestProject/Spine/Solder/Solder.lean` — the isolated missing datum `TangentSolderData` (fibrewise linear equivalences `LocalModel ≃ₗ[ℝ] T_xM` with exactly the Lorentz overlap law; regularity of the comparisons is derived, not assumed), the transported tangent Lorentz metric with chart-independence, nondegeneracy, frame isometry and dimension 4, non-vacuity of the datum, and the exact residual gauge freedom (a torsor under pointwise tangent automorphism fields — so no solder field is canonical).
5. `RequestProject/Spine/Solder/LorentzBundle.lean` — the associated `InternalLorentzBundle` as a genuine Mathlib `VectorBundleCore` whose transition functions are exactly the projected Lorentz action, plus the fibrewise equivalence and the intertwining square.
6. `RequestProject/Spine/Comparison/SolderTangentGate.lean` — the single comparison leaf: derived orientation and time orientation, genuine `LorentzFrames.LorentzTangentFrameData` on the emergent manifold, the principal square `tangent frame cocycle = projected native Lorentz cocycle`, and the native Spin lift of the *actual* tangent Lorentz frame data. Nothing about `w₁`, `w₂`, connections or curvature is claimed; the topological bundle isomorphism to `TangentBundle` is deliberately not packaged, with the exact missing ingredient (frame regularity) named.

**Architecture.** The bottom-up branch never imports the top-down tangent/frame machinery; the mechanical firewall in `Spine/Audit/ArchitectureDAG.lean` was extended (check 9 now covers `Emergent.TangentTransition`, new check 10 covers the four `Spine.Solder` modules and requires the comparison module to see both branches). One pre-existing exemption list was widened for the solder layer, recorded in the source and in the failbuild ledger.

**Documentation repair.** `TASK34_PROVENANCE.md`, `TASK34_AUDIT.md`, `TASK34_CODE_HYGIENE.md`, `TASK34_FAILBUILDS.md` (twelve recorded failures with command, error, diagnosis, repair and impact) and `TASK34_DOCUMENTATION_REPAIR.md` (repository-wide terminology audit classifying every surviving occurrence of the flagged phrases). Correction notices were added to `TASK32_PROVENANCE.md`, `TASK32_AUDIT.md`, `TASK32_CODE_HYGIENE.md`, `TASK33_PROVENANCE.md`, `TASK33_AUDIT.md` and to the docstring of `symmetric_emergent_manifold`, preserving every historical statement verbatim; `README.md`, `PROVENANCE_LEDGER.md`, `COMBINED_PROJECT_PROVENANCE.md`, `ARCHITECTURE.md` and `DEPENDENCY_DAG.md` were updated. As instructed in this session, `ARISTOTLE_SUMMARY.md` was left unedited; its Task-32 paragraphs are historical and their corrections are the ones recorded in the Task-34 documents.

# Summary of changes for run a489983c-356d-4e7c-9601-4da47aa0ebdf
**Outcome A — the smooth-manifold gate is closed and the canonicity/minimality claims are now exactly as strong as the theorems.**

Four new modules were added under `RequestProject/Spine/Emergent/`, all of them sorry-free and building from a clean `rm -rf .lake/build && lake build RequestProject`:

1. `SmoothStructure.lean` — the missing bridge. It first identifies the charts that the existing reconstruction actually produces: `chartOfPoint p` is (definitionally) the chart of the index carried by the representative the quotient machinery selects (`chartOfPoint_eq_pieceChart`), and for two atlas members the coordinate change `e.symm ≫ₕ e'` has the incidence domain `W i j` as its *exact* source (`transition_source`) and equals the primitive map `φ i j` there (`transition_apply`). From this, `BaseGluingData.isManifold_of_smoothGluing` proves that `SmoothGluing` implies Mathlib's `IsManifold (modelWithCornersSelf ℝ LocalModel) ⊤ (Space B)` for the *existing* charted-space structure (registered as an instance; no second atlas), with the `HasGroupoid` form derived. `smoothEmergentManifoldCertificate` packages Hausdorff + second countable + charted + smooth + 4-dimensional local model, keeping the topological conditions outside `IsManifold` as Mathlib does. A negative control (`nonSmoothGluing`, `not_smoothGluing_nonSmoothGluing`, `nonSmoothGluing_topological_reconstruction`) shows the smoothness condition is genuinely extra. The *converse* is explicitly not claimed, with the reason recorded: the generated atlas contains only the charts of the selected representatives' indices.
2. `SymmetricCanonical.lean` — genuine canonicity replacing the earlier `Nonempty`-of-homeomorphisms statement: the intrinsic quotient coordinate obtained by descent (no index chosen), `chart i x = chart j x` for all indices, two-sided inversion, the distinguished `baseHomeomorph`, the named `symmetricCanonicalHomeomorph`, and the `refl`/`symm`/`trans` coherence laws.
3. `SymmetricSmooth.lean` — the same comparison in the smooth category: the symmetric emergent base is a C^∞ manifold, every chart it produces is the intrinsic coordinate, and `symmetricCanonicalDiffeomorph` is a `Diffeomorph` with the three coherence laws, under no new assumption.
4. `LocalPieceData.lean` — the forgetful local-piece layer, with `base_incidence_not_determined_by_local_piece_data` and the stronger `no_reconstruction_from_local_piece_data` (no assignment from local-piece data to spaces reproduces the base up to homeomorphism).

Documentation was repaired to match the theorems: "the minimal missing primitive" → "an explicit sufficient base-gluing primitive whose cross-piece incidence layer is irreducibly additional"; "canonical homeomorphism" in the earlier symmetric statement downgraded to "existence", with pointers to the new canonical results; the Spin statement reworded as *the same pointwise trivial transition law on two different covers* (they are not one typed object, and no type-erasing wrapper was manufactured); and the Čech H¹ result explicitly scoped to the fixed symmetric full-overlap cover. The compile-time architecture audit now runs the base-free firewall over ten emergence modules, so the smooth layer cannot import the top-down tangent/frame or Spin-obstruction branches — no architecture regression occurred. `#print axioms` on every new endpoint reports only `propext`, `Classical.choice`, `Quot.sound`. Reports are in `TASK33_PROVENANCE.md`, `TASK33_AUDIT.md`, `TASK33_FAILBUILDS.md`, `TASK33_CODE_HYGIENE.md`. Nothing about tangent bundles, solder forms, w₁ or w₂ is introduced.

# Summary of changes for run 52027f6d-5412-4f61-a981-7e3b56d78824
**Task 32 — manifold-emergence gate. Outcome B, refined by C; Outcome E excluded in the symmetric sector.** Everything below is proved in Lean against the frozen toolchain and Mathlib revision (both untouched), with no `sorry`, `admit`, new `axiom`, `native_decide`, `unsafe`, `partial` or `implemented_by`; `rm -rf .lake/build && lake build RequestProject` completes cleanly (8309 jobs) and every `#print axioms` on the new endpoints reports only `propext`, `Classical.choice`, `Quot.sound`.

**Audit first.** Reading the existing sources answered the seven provenance questions: the first primitive mentioning a base (and requiring its topology) is the cover structure of `Spine/E2/Cech/Cover.lean`; overlaps there are *derived* from an already existing point set, so no base-free incidence object existed anywhere below it; the Clifford/Spin core already supplies a canonical base-free 4-dimensional Lorentz model (the intrinsic carrier `ℝ ⊕ ℝ³`), and no theorem connected that model to charts or tangent spaces without presupposing a manifold. Details and the arrow-by-arrow chain are in `TASK32_PROVENANCE.md`.

**Answers to the two stop-condition questions.** (1) *What extra information is needed between the local core and a global 4-manifold?* Exactly one datum: local domains, an incidence relation between them, base-point identification maps and their coherence laws — packaged as `EmergentBase.BaseGluingData` (`Spine/Emergent/BaseGluing.lean`). That it is irreducible is a theorem, not an assumption: `EmergentBase.base_not_determined_by_local_pieces` and `SpinNative.spin_data_do_not_determine_base` exhibit two gluing data with the *same* local pieces carrying *literally the same* (trivial) Spin transition datum whose bases are not homeomorphic (one connected, one not). Fibre gluing is therefore provably not base gluing. From that one primitive the base is reconstructed: the tagged disjoint union modulo the gluing relation, with an **open** quotient map, open chart embeddings, a genuine `ChartedSpace` over the 4-dimensional local model, Hausdorffness under an explicit closed-graph condition, and second countability for a countable index type (`Spine/Emergent/Reconstruction.lean`, endpoints `emergentManifold`, `emergentManifoldCertificate`). Smoothness is *not* claimed: the change-of-chart law shows the atlas transitions are exactly the primitive's identification maps, so the exact missing hypothesis is isolated as `SmoothGluing` (`Spine/Emergent/SmoothGate.lean`). (2) *Does the symmetric global structure remove the Task-31 kernel/H¹ Spin ambiguity?* Yes. In the neutral sector the emergent base is canonically homeomorphic to the local domain, connected, Hausdorff, second countable and 4-dimensional, and unique up to canonical homeomorphism; its emergent cover has full nerve, so the fixed-cover Čech Ȟ¹(𝓤;ℤ₂) **vanishes** (new generic lemma `CechZ2.cohomology_one_eq_zero_of_fullNerve`), the Task-31 gate holds without being assumed, and there is exactly one Spin gauge class (`SpinNative.symmetric_emergent_spin_unique_up_to_gauge`, `emergent_seed_certificate`).

**Removal of the external base.** `SpinNative.emergentSeed` produces a canonical Spin seed whose base space and cover are *constructed* from the gluing datum, so the previous external inputs (base set, its topology, the cover) are no longer parameters; the Task-30 statement that the standard Spin-lift obstruction of the projection is trivial holds verbatim over the emergent base, so the standard top-down control branch survives unchanged and independent. The comparison edge to `TM`/`P_SO(TM)` is classified **requires solder form** and is not faked. No connection, transport, holonomy, curvature or dynamics appears anywhere in the new layer or its imports.

**Architecture.** Nine new modules (`Spine/Emergent/*`, `Spine/Cech/FullNerve.lean`, `Spine/Comparison/EmergentSpinGate.lean`, 1 286 lines), no existing mathematics edited, nothing duplicated. `Spine/Audit/ArchitectureDAG.lean` gained a mechanical firewall for the new layer with both negative and positive controls: the six base-free modules reach no cover/Čech/obstruction/geometry/Spin-native module, the single adapter may see the cover interface and nothing above it, and the comparison join is a leaf that genuinely reaches both branches. The audit prints "all checks passed". Reports: `TASK32_PROVENANCE.md`, `TASK32_CODE_HYGIENE.md`, `TASK32_FAILBUILDS.md` (nine recorded failures, including one abandoned refactor), `TASK32_AUDIT.md`.

# Summary of changes for run 5a05c673-5adb-4751-9ba7-30fd6dea36d7
**Task 31 — canonical Spin-structure gate. Outcome B, refined by Outcome C.** All new mathematics is proved in Lean 4.28.0 against the pinned Mathlib revision (toolchain and manifest untouched), with no `sorry`, no new axiom, no `native_decide`, `unsafe`, `partial` or `implemented_by`.

**Audit first.** Inspecting the existing machinery showed that the difference of two Spin structures with the same SO projection was measured only by `LorentzFrames.SpinFrameStructure.ratio` into a raw fixed-cover `{±1}`-valued Čech 1-cocycle; the project had a fixed-cover *torsor* statement, no quotient, no equivalence relation on Spin structures, and no H¹ classification. Nothing was duplicated: the new generic `SpinNative.KerCocycle` is the same structure, identified with the Lorentz-frame one by mutually inverse `rfl` maps, and the missing degree-0 nerve API was added to the existing `Cech/CoverNerve.lean`.

**Answer.** (1) *Canonical selection holds*: `SpinNative.canonicalSpinStructure` produces one distinguished Spin structure from coherent Spin-native gluing data, with the datum itself as carrier and `rfl` as compatibility — no SO-side lift, no local section of ρ, no choice. (2) *Native determinism holds*: `canonicalSpinStructure_unique`, `subsingleton_native_output`. (3) *Genuine uniqueness does not hold by provenance alone*: over the same projection the data form a torsor under kernel-valued Čech 1-cocycles (`exists_kerCocycle_twist_eq`), and the twist is invisible after projection (`project_twist`). (4) The symmetric ("null") Spin gluing state does **not** remove this freedom — on a two-patch cover with a nontrivial kernel element there really are two distinct Spin structures over the same projection (`SpinNativeControl.two_distinct_native_spin_structures`, with the intrinsic Spin instance), so strict rigidity provably fails there; on that cover, however, the freedom is pure gauge (`two_structures_gaugeEquiv`). (5) Two named rigidity conditions were isolated — `NoNontrivialKernelTwist` (pointwise) and `KernelTwistsGaugeTrivial` (the fixed-cover H¹-type gate) — each stating exactly what it assumes, both shown non-vacuous and the first shown falsifiable; from them come uniqueness on overlaps, uniqueness up to gauge equivalence, and `Subsingleton (GaugeClass …)`. (6) The gate is reduced, theorem-level, to the vanishing of the project's own nerve-level Čech Ȟ¹(𝓤;ℤ₂) of the *given* cover on covers with preconnected overlaps (`kernelTwistsGaugeTrivial_of_cover_H1_trivial`), with a Lorentz-frame instance for `SpinFrameStructure`. Identification with a global H¹(X;ℤ₂) is **not** claimed; the missing steps (Čech-to-singular comparison, refinement invariance) are documented.

**Deliverables.** Eight new modules — `Spine/SpinNative/{KernelTwist, GaugeEquivalence, Canonical, Rigidity}.lean`, `Spine/Comparison/{KernelTwistCohomology, LorentzSpinUniqueness, CanonicalSpinSeed}.lean`, `Spine/Controls/SpinNative/KernelTwistControl.lean` — plus the packaged handoff `SpinNative.CanonicalSpinSeed` with `seed_certificate`, whose parameters make the still-external inputs (base, topology, index type, cover) syntactically obvious. No manifold, connection, transport, holonomy or curvature was introduced. Both branches are preserved and the firewall was extended: the uniqueness layer cannot reach the SO-first lift construction, the obstruction machinery, the cohomology, the Nerve or the frame geometry, and this is enforced by new checks in `Spine/Audit/ArchitectureDAG.lean`. Documents `TASK31_PROVENANCE.md`, `TASK31_CODE_HYGIENE.md`, `TASK31_FAILBUILDS.md`, `TASK31_AUDIT.md` record the dependency chain, the metrics, every failed build, and the axiom profile. `rm -rf .lake/build && lake build RequestProject` completes successfully (8300 jobs, all audits passing) and every principal endpoint prints `[propext, Classical.choice, Quot.sound]` — the established baseline, with no `sorryAx`.

# Summary of changes for run b9dd57b0-bfcf-4a09-b862-2cc154bb1cf7
**Task 30 — Outcome A.** The Spin-native provenance branch is in place, the standard SO-lift control path is untouched, and the principal implication is proved.

**Audit first (§3).** Inspecting the existing tree showed: the earliest genuinely Spin-valued primitive is the intrinsic spin group `SpinCore.SpinGroup` with `spinCover` (a *local fibre* object); but every pre-existing Spin-valued *gluing* object — `CechSpinLift.SpinLiftFamily`, `LorentzFrames.SpinFrameStructure`, `NullSectorTask29.CompatibleContinuousInternalTransitions` — takes the SO datum as a parameter or constrains its Spin maps by a projection field, so none of them is Spin-native. However, the generic cocycle structure `CechSpinLift.VisibleCocycle` is stated for an *arbitrary* topological group and never mentions ρ; only its name carries the SO-first reading. So no new structure was invented: the Spin-native datum is that existing structure instantiated at the Spin group, exposed as `SpinNative.NativeSpinTransitionData`.

**New mathematics (3 modules, minimal).** `Spine/SpinNative/TransitionData.lean` (Spin-valued gluing data; identity/inverse laws re-exported, not reproved), `Spine/SpinNative/Projection.lean` (`SpinNative.project`: g_ij := ρ ∘ g̃_ij is again an exact cocycle; `proj_ker_mul`; `project_eq_of_ker_factor`, the information-loss statement), and the leaf comparison layer `Spine/Comparison/SpinNativeVsSO.lean`. There: Spin-native data are a family of local lifts of their own projection (`nativeLiftFamily`), the standard triple-overlap defect of that family is identically 1, and — the principal target — `SpinNative.projected_obstruction_trivial_of_lifts`: the **existing, unmodified** standard obstruction `CechSpinLift.SpinLiftFamily.obstruction` of the projected data equals `trivialClass`, for *every* choice of local lifts. Instances/consequences: `SpinCore.spin_projected_obstruction_trivial` (intrinsic Spin/Lorentz cover), `SpinFrameStructure.toNative`, `LorentzFrames.project_toNative_eq_transition`, and `LorentzFrames.project_twist` (`project(ε·S) = project(S)` on the overlaps — the global form would be false, since ε is only known kernel-valued there).

**Answers.** Q1: yes at the fibre level (`SpinGroup`), no at the gluing level before this task. Q2: the SO-first edge is the parameter `(T : VisibleCocycle G 𝓤)` of `SpinLiftFamily` in `E2/Cech/LocalLifts.lean`. Q3: yes — coherent Spin gluing is reached with no SO→Spin reconstruction, no local section, no lift-admissibility hypothesis. Q4: yes, theorem-level. Q5: existence information retained (proved), kernel-sign/choice information lost (proved), global H¹-freedom **not** classified and not claimed. Q6: the SO-first packaging is historical, not mathematically necessary — proved by the group-agnosticism of the cocycle structure.

**Architecture, build, docs.** The DAG now branches explicitly; `Spine/Audit/ArchitectureDAG.lean` gained a mechanical branch firewall (Spin-native modules reach no defect/coboundary/obstruction/refinement/geometry/nerve/controls module; the standard branch does not reach the Spin-native one; the comparison layer is a production leaf; positive controls prevent vacuity). A cold build (`rm -rf .lake/build`, `lake build RequestProject`) reports "Build completed successfully (8292 jobs)", 0 errors, with both audits reporting all checks passed. No `sorry`/`admit`/new `axiom`/`native_decide`/`unsafe`/`partial`/`implemented_by`; toolchain, lakefile and manifest untouched. `#print axioms` on all thirteen new endpoints yields exactly `[propext, Classical.choice, Quot.sound]`; the axiom table (`scripts/task30_axioms_post.txt`, 1037 declarations) differs from the 1024 baseline by exactly those thirteen. Metrics (`scripts/task30_metrics_post.txt`): modules 261→264, LOC 51919→52388, internal edges 442→448, **maximum import depth unchanged at 67**. Documents written: `TASK30_PROVENANCE.md` (source-derived DAGs for both branches, every arrow classified, all six questions answered), `TASK30_CODE_HYGIENE.md`, `TASK30_FAILBUILDS.md` (the one build failure — a collision with the inherited Stage-1.2 sign theorem — plus the rejected routes), `TASK30_AUDIT.md`, and a Task-30 addendum to `DEPENDENCY_DAG.md`. The Nerve Theorem, the w₂(TM) identification and Stage 1.4 were deliberately not begun.

# Summary of changes for run ddb53dd3-fe11-4058-8d0f-9912720288a2
Task 29 is complete with **Outcome A**: the global simplicial–singular comparison begun earlier is now finished, integrated, built and certified, with no new assumptions.

**Mathematics closed**
- `NerveTopology.exists_skeletal_factorization` (a continuous map from a compact space into |K| factors continuously through a finite skeleton) was inherited but did not compile; it was repaired locally with proof-only fixes, and its domain universe was generalized so it can be applied to the standard topological simplex. Its mathematical statement is unchanged.
- `NerveSkeleton.skeletalSupport (K : SSet) : SkeletalSupport K` is now proved unconditionally, by instantiating the factorization theorem at the compact domain of a singular simplex. No `Fintype`/`Finite`/dimension bound/finite cover hypothesis is used; the outdated documentation calling skeletal support an external assumption has been removed.
- `NerveComparison.globalHomologyIso (K : SSet) (q : ℕ)`: the frozen canonical chain map J_K = C_*(η_K) induces an isomorphism on mod-2 homology in every degree, for arbitrary simplicial sets. The earlier finite-dimensional statement is recorded as a corollary rather than an independent route.
- New generic algebra, placed in the lowest layer with no nerve/geometry imports: an elementary homology layer (`SpineNaiveHomology`) bridging the categorical isomorphism to an elementary bijection, and a mod-2 duality theorem `SpineDualCohomology.bijective_Hmap` (a chain map of free ℤ₂-complexes that is a homology isomorphism transposes to a cohomology bijection). The pre-existing dualisation results were consolidated to re-export this single generic copy.
- `NerveGeom.geometricHmap_bijective` and hence `NerveGeom.geometricComparison : GeometricComparison U`, unconditionally, for the already-frozen map — no replacement map, no assumed chain-homotopy equivalence, no finiteness.

**Architecture, build and audits**
- The architecture audit no longer treats `RelJ` as an absolute leaf; it now enforces the layering lower AlgebraicTopology → lower nerve geometry → RelJ → global homology → global cohomology, with positive controls, and reports all checks passed. No import cycles, no umbrella-import shortcuts, no downward dependencies.
- A genuine cold build (`rm -rf .lake/build`, then a full build) succeeded with 0 errors. No `sorry`, `admit`, new `axiom`, `native_decide`, `unsafe`, `partial` or `implemented_by`; the toolchain, manifest and Mathlib pin are untouched. `#print axioms` on every new endpoint yields only `propext`, `Classical.choice`, `Quot.sound`; the axiom table has 1024 audited declarations versus the 1010 baseline, the difference being the new endpoints plus renamed dualisation lemmas. All required regression endpoints remain valid and unweakened.
- Metrics were measured from the actual trees: Spine modules 258 → 261, Spine LOC 51052 → 51919; Stage-1.3 modules 95 → 98, LOC 19401 → 20222, internal import edges 158 → 165, maximum depth 24 → 26 (justified by the two genuinely new dependency layers); transitive closure of global homology 71 → 75, of the final cohomology endpoint 182.
- Documents written: `TASK29_AUDIT.md` (separating inherited source, repairs, and genuinely new mathematics), `TASK29_CODE_HYGIENE.md`, `TASK29_FAILBUILDS.md` (all failed builds and abandoned routes retained), plus an addendum to `DEPENDENCY_DAG.md` and metric/axiom records under `scripts/`.

Per the stop condition, the Nerve Theorem and the w₂ identification were deliberately not begun.

# Summary of changes for run d67c72d0-37ff-43d5-912d-d45227123e64
**Outcome A — arbitrary-family B3 is closed.** The remaining finiteness obstruction has been removed; the full project builds (`lake build RequestProject`: "Build completed successfully (8276 jobs)", 0 errors, no `sorry`/`admit`/`native_decide`/`unsafe`/`@[implemented_by]`/project-local `axiom`), on the unchanged pin (`lean-toolchain` and `lake-manifest.json` untouched).

**1. The generic theorem.** A new module `RequestProject/Spine/AlgebraicTopology/DirectSumHomology.lean` proves, for an *arbitrary* index type `α`,
`SpineTask23.isIso_sumHomologyMap_finsuppCxι : IsIso (sumHomologyMap (finsuppCxι α C) q)`, i.e. `H_q(⊕_α C) ≅ ⊕_α H_q(C)` for the project's own `Finsupp`-based direct sum and its own comparison map. The route is an explicit local-finiteness argument: injectivity is formal (the orthogonal retractions recover each coefficient); surjectivity takes a cycle representative of a class (`homologyπ` is epi), uses the finite support of its underlying chain to fix it by a *finite* partial idempotent `∑_{s∈S} P s ≫ ι s` (`iCycles` is mono), and turns that into the required finite sum by additivity of the homology functor. The general splitting criterion `isIso_sumHomologyMap_of_splitting` and the transported form `isIso_sumHomologyMap_of_iso` (now without `Fintype`) are also public. `#check` confirms no `Fintype`, `Finite` or `DecidableEq` binder survives in any principal statement; `classical` is used only for `Finset`/`Finsupp` bookkeeping.

**2. Application, reusing existing work.** The already existing arbitrary-family chain isomorphism `Θ_rel`, the standard-cell/open-cell comparison and the excision compatibility were reused unchanged; no geometry was rebuilt. This gives `SpineTask24.isIso_sumCellHomology` and `SpineTask24.isIso_tgtDecomp` with no finiteness hypothesis, and hence the endpoint `SpineTask24.relJIsIso : SpineTask14.RelJIsIso X r` for every simplicial set and every `r`. Nothing beyond B3 was attempted.

**3. Hygiene and architecture.** The generic module sits below `Nerve` (its whole project-local closure is 4 algebraic modules) and the join module was renamed `Nerve/Comparison/FiniteRelJ.lean → Nerve/Comparison/RelJ.lean` to match its content, staying a leaf. Exactly two new import edges were introduced (both mathematically forced); the finite splitting criterion was re-proved as a corollary of the general one and two obsolete finite-only helpers were deleted, so no finite/arbitrary proof logic is duplicated; the finite wrappers (`isIso_tgtDecomp_finite`, `relJIsIso_finite`, …) are retained with unchanged statements. The architecture audit was strengthened (an explicit no-reach check for the new module, and the endpoint control now also requires it) and reports "SPINE ARCHITECTURE AUDIT: all checks passed". Metrics: modules 84→85, LOC 17575→17724, internal edges 143→145, **max import depth unchanged at 24**. The axiom table (1010 declarations) differs from the previous baseline by exactly the seven new declarations, all `[propext, Classical.choice, Quot.sound]`, no `sorryAx`.

**4. Documents.** `TASK27_AUDIT.md`, `TASK27_CODE_HYGIENE.md` and `TASK27_FAILBUILDS.md` were written as specified (including the two build failures, their diagnoses and repairs — neither changed a statement or the import DAG), metrics snapshots are in `scripts/task27_metrics_pre.txt` / `_post.txt` and the axiom table in `scripts/task27_axioms_post.txt`, and a Task-27 addendum was appended to `DEPENDENCY_DAG.md`. Frontier: B1 CLOSED, B2 CLOSED, B3 finite CLOSED, B3 arbitrary CLOSED; next edge is the global skeletal assembly / canonical simplicial–singular comparison. All work is committed.

# Summary of changes for run 731a68b3-ea06-47a4-9144-6e7c737538de
Continued Task 25 from the existing refactored sources, with no module moves redone and no new mathematics: the only Lean edits made were `import` lines.

**1. Green build.** The refactored tree did not compile. `RequestProject/Spine/Nerve/StandardCell/Excision.lean` failed with 15 errors (starting from `invalid use of explicit universe parameters, 'stdCellPair' is a local variable`) because the split-out definition module `Nerve.StandardCell.Pair` was no longer in its import closure; adding that one import fixes it. After the repair, `lake build RequestProject` from a deleted build directory reports **"Build completed successfully (8275 jobs)"** with 0 errors, no `sorry`/`admit`, no `native_decide`, no `unsafe`, no `@[implemented_by]` and no project-local `axiom`; `lean-toolchain` and `lake-manifest.json` are unchanged.

**2. Import narrowing finished.** Four further project-local import edges were removed and re-verified green (`CellFamily.TargetMap` → `RelativeChainCalculus`, `Comparison.BaseCase` → `Skeleton.RealizationMono`, `Comparison.RelativeMap` → `RealizationColimits`, `StandardCell.BoundaryHomology` → `SingularHZero`). One further proposal (dropping `SingularHomology` from `SingularHomotopy`) was tried, shown to break the build, and rejected as a false positive of the name-mention heuristic; the edge is kept.

**3. Exact post-refactor metrics** (`scripts/task25_metrics_post.txt`, compared against the recorded pre-refactor snapshot): 84 modules / 17575 LOC / 143 internal import edges in the Stage-1.3 area, and the maximum internal import depth drops from **45 to 24**. Endpoint closures shrink like-for-like: 57→46, 65→47, 69→39 for the three principal endpoints; the final endpoint `Nerve.Comparison.FiniteRelJ` has a closure of 61.

**4. Audits run.** `Spine/Audit/ArchitectureDAG.lean` passes all six checks on the compiled import graph (78 Stage-1.3 production modules audited, 0 chronological `TaskNN` modules, 0 domain-specific imports from the 28 generic modules, 0 high-level imports from the 18 low-level ones), and the legacy firewall still reports 0 experiment-tree imports across 173 Spine modules. The axiom audit over the full build log covers 1003 distinct declarations (1000 with `[Classical.choice, Quot.sound, propext]`, 2 with `[Quot.sound, propext]`, 1 with `[propext]`, 0 with `sorryAx` or anything else) and is **byte-identical to the pre-refactor baseline**, which is the mechanical evidence that the refactor changed no declaration and no logical dependency.

**5. Documents created**: `TASK25_ARCHITECTURE_AUDIT.md` (architecture rule, layer layout and sizes, layer-level edge table with the two group-level back-edges explained, the six mechanical checks with their reported numbers, pre/post metrics, the narrowing table, and the axiom audit) and `TASK25_FAILBUILDS.md` (both failed compilations with exact Lean errors, diagnosis, repair, and the note that no statement changed). Supporting data are in `scripts/task25_metrics_post.txt` and `scripts/task25_axioms_post.txt`. All work is committed.

# Summary of changes for run c0e24549-9490-4ba9-8c0d-3b57cfc00510
**Outcome A — the finite-scope completion of Task 23 is done.**

`IsIso (tgtDecomp X r q)` is proved for every `q` under `[Fintype ↑(X.nonDegenerate r)]`, with `tgtDecomp` the *frozen* Task-22 map (no replacement definition), and the finite-family `SpineTask14.RelJIsIso X r` is derived from it. The whole project builds green (`lake build RequestProject` → "Build completed successfully (8271 jobs)", 0 errors, firewall audit passed), with no `sorry`, `admit`, new axiom, `native_decide`, `unsafe` or `@[implemented_by]`, and no change to `lean-toolchain` or `lake-manifest.json`.

**Inherited-source repair.** `Spine/Nerve/Task23CellSum.lean` did **not** compile in the pinned environment: `exists_cellIncl` failed with *"don't know how to synthesize implicit argument `σ`"*. A one-token, proof-only repair (`(σ := σ)`) fixes it; no statement changed. `Task23FinsuppComplex.lean` compiled unchanged. Both post-audit modules are now part of an actual verified build, and both were reused rather than reconstructed.

**New work (two modules plus an axiom audit).**
- `Spine/Nerve/Task24RelativeCellSum.lean` (WP1–WP4): `finsuppCxDesc` (map out of `SpineTask23.finsuppCx` from a family of chain maps, with `finsuppCxι_desc`); `famChainMap` and **Θ_abs** `thetaAbs`, an actual chain-complex isomorphism `⊕_σ C_*^sing(Δ°) ≅ C_*^sing(U)` — boundary compatibility holds because every component is the chain map induced by a simplicial map (a face of a simplex in the cell of σ stays in that cell); degreewise bijectivity comes from the coordinate formula `Θ_abs(d)(cellSingMap (σ,s)) = d σ s` plus `bijective_cellSingMap`. `famRelChainMap` and **Θ_rel** `thetaRel`, an actual isomorphism of the project's *existing* relative chain complexes `⊕_σ C_*^sing(Δ°, Δ°∖{b_r}) ≅ C_*^sing(U, U∩V)`, whose only relative input is the inherited `cellSingMap_mem_range_incInterU_iff`. Then WP4: `isIso_sumCellHomology`, the finite relative homology decomposition, via the inherited `isIso_sumHomologyMap_of_iso`. `Fintype` is used only here.
- `Spine/Nerve/Task24FiniteTargetDecomp.lean` (WP5–WP8): transport along the inherited `stdCellIso` and `cellPairIso` (`excision_cell_square`, `stdPair_cell_square`, `cell_decomposition_square`), the identification `mapRangeStd_tgtDecomp` (extensionality on `Finsupp.single`, reducing componentwise to the fact that both maps are induced by the same cell inclusion), `isIso_tgtDecomp_finite`, and `relJIsIso_finite` — the last a pure corollary of the frozen Task-22 square `srcDecomp⁻¹ ≫ ⊕_σ H_*(cellRelJ) ≫ tgtDecomp`.
- `Spine/Nerve/Task24AxiomAudit.lean`: `#print axioms` for every principal inherited and new declaration; all report exactly `[propext, Classical.choice, Quot.sound]`, no `sorryAx`.

**Documentation.** `TASK24_AUDIT.md` records the provenance repair (Task 23 out of budget; the two modules missing from the earlier audit; reuse rather than reconstruction; which module needed repair), the full list of reused Task-23/22 declarations, the exact constructions of Θ_abs and Θ_rel, boundary compatibility, the finite homology decomposition, the equality with `tgtDecomp`, the two endpoint theorems, a classification of declarations as inherited / repaired / new, and the build and axiom audit. `TASK24_FAILBUILDS.md` records all seven failed builds (inherited-module failure, `Finsupp.sum_single_index`/`mapRange_single` unification, `ModuleCat` carrier-coercion obstructions to the coordinate formula, uninferable index type, invalid `▸`, and the `tgtDecomp` identification), each with the exact error, diagnosis, repair, and whether a statement changed — only auxiliary degreewise lemma statements changed, no principal statement.

**Scope respected.** The arbitrary-family `RelJIsIso X r` is neither proved nor assumed; the `Fintype` hypothesis is retained everywhere, and no arbitrary-family, filtered-colimit, wedge, skeletal-induction or downstream work was begun.

# Summary of changes for run a2b810e0-c86a-4d12-9aec-c3209c397136
## Task 22 — outcome C (with the `J`-compatibility square additionally closed)

Everything below is machine-checked at the frozen pin (Lean v4.28.0, Mathlib rev `8f9d9cff…`); `lean-toolchain`, `lakefile.toml` and `lake-manifest.json` are untouched, no existing module was modified, and the comparison `J = C_*(η)` (`SpineTask14.relJ`, `SpineTask20.cellRelJ`) is used unchanged. `lake build RequestProject` → **Build completed successfully (8258 jobs)**, including the pre-existing spine firewall audit. No `sorry`, `admit`, new `axiom`, `@[implemented_by]` or `native_decide`; `#print axioms` on every principal new declaration reports exactly `[propext, Classical.choice, Quot.sound]`.

### What is proved

New modules, all in namespace `SpineTask22`:

* `RequestProject/Spine/Nerve/Task22FiniteCellAdditivity.lean`
  * `isZero_skelRel_homology` — `H_q^simp(Sk X (r+1), Sk X r; ℤ₂) = 0` for `q ≠ r`, from the Task-13 relative-normalized vanishing plus the (Task-21) natural normalization homotopy;
  * `bijective_phi` — in the critical degree the map `σ ↦ [σ]` from `(X.nonDegenerate r →₀ ℤ₂)` to `H_r^simp(Sk X (r+1), Sk X r; ℤ₂)` is bijective, from the Task-13 free-basis computation (`normChainEquiv`, `nonDegenerateSkeletonEquiv`);
  * **`isIso_srcDecomp` / `srcDecompIso`** — the source finite-family decomposition `⊕_{σ ∈ X.nonDegenerate r} H_q^simp(Δ[r],∂Δ[r];ℤ₂) ≅ H_q^simp(Sk X (r+1), Sk X r;ℤ₂)`, realised by the canonical Task-15 characteristic maps `cellChar X r σ` (no noncanonical isomorphism is chosen).
* `RequestProject/Spine/Nerve/Task22RelJCompatibility.lean`
  * `cellPairMap_relJ` — chain-level componentwise compatibility, both sides being the relative comparison attached to the unit-naturality square of `cellChar X r σ`;
  * **`relJ_decomposition_square`** — the comparison square commutes: `srcDecomp ≫ H_q(relJ X r) = (⊕_σ H_q(cellRelJ r)) ≫ tgtDecomp`, the bottom map being exactly the Task-21 single-cell comparison on every component; also `homologyMap_relJ_eq` and `isIso_sumCellRelJ`;
  * generator-level form `relJ_f_cellSimplex`, `relJ_topCls`, **`relJ_generator_component`** (`e_σ ↦ the σ-summand of stdCellGenerator r`), all derived from the frozen `relJ_generator`.
* `RequestProject/Spine/Nerve/Task22CellSeparation.lean` — the §7 geometric separation statement, proved: `compl_range_skInc` says the complement of `|Sk X r|` in `|Sk X (r+1)|` is the image of the complement of `∐_σ |∂Δ[r]|`, with `real_cellMap_injOn_compl_bdry` giving injectivity there — i.e. the open cells of distinct nondegenerate `r`-simplices are pairwise disjoint and disjoint from the old skeleton. Inputs: only the Task-15 realized pushout and the unconditional Task-16 realization monomorphism; no local finiteness, no closure-finiteness, no CW theory.
* `RequestProject/Spine/Nerve/Task22AxiomAudit.lean` — `#print axioms` for every principal declaration.

### What is not proved

WP3, the **singular** finite-family decomposition, is not reached. The map `tgtDecomp` is constructed, but the statement that it is an isomorphism is only recorded as the proposition `SingularCellFamilyAdditivity`, which is never assumed. Task-18 Excision could not be applied: it needs an *open* excisive family in `|Sk X (r+1)|`, and only the set-level separation is available. The remaining topological inputs are listed exactly in `TASK22_AUDIT.md` §14 (openness characterisation of the realized pushout; a radial deformation retraction of the punctured cells onto the boundary, descended through the pushout; relative homotopy invariance together with finite additivity of relative singular homology over a topological coproduct).

Only one conditional corollary was produced, marked as such: `relJIsIso_of_singularCellFamilyAdditivity` (finite-cell-family framing only, and conditional on the open statement above). Arbitrary-family B3 remains open, `SpineTask14.RelJIsIso` is untouched, and no skeletal induction was started.

### Finiteness

Reported honestly rather than assumed: **no finiteness hypothesis was needed**. The direct sum is taken as the `Finsupp` coproduct `(X.nonDegenerate r →₀ M)`, which is the coproduct for any index type, so the source decomposition, the comparison square and the generator statements hold for an arbitrary family. The finite-family framing is still available, as `finiteBiproduct_of_fintype` identifies the same object with the finite biproduct. No arbitrary-coproduct-preservation theorem, no cellular homology, no wedge axiom and no additivity over arbitrary coproducts is used anywhere.

`TASK22_AUDIT.md` answers all fourteen required audit points; `TASK22_FAILBUILDS.md` records all thirteen failed elaborations with diagnosis and repair, and the two routes abandoned because they would have required a forbidden strengthening.

# Summary of changes for run 9acd6314-5459-40c9-8bd7-930853f69022
**Task 21 — Outcome A: the single standard-cell relative comparison is closed.**

For every `r` and **every** degree `q`, the already-frozen relative comparison `SpineTask20.cellRelJ r` (i.e. `J = C_*(η)` for the adjunction unit, unchanged and not replaced) is proved to induce an isomorphism

H_q^simp(Δ[r], ∂Δ[r]; ℤ₂) ≅ H_q^sing(|Δ[r]|, |∂Δ[r]|; ℤ₂).

Everything is machine-checked at the frozen pin (Lean v4.28.0, Mathlib rev 8f9d9cff…; toolchain, `lakefile.toml` and `lake-manifest.json` untouched, no existing module modified).

New modules (all in namespace `SpineTask21`):

* `RequestProject/Spine/Nerve/Task21Normalization.lean` — the one missing input: the Dold–Kan homotopies `P q ≃ id` and `P∞ ≃ id` are natural, hence the Task-13 normalization homotopy is natural (`normHtpy_natural`).
* `RequestProject/Spine/Nerve/Task21SingleCellRelJ.lean` —
  * audit finding: the *simplicial* relative homology of `(Δ[r], ∂Δ[r])` did not exist anywhere in the project, so it is computed here directly at chain level: every simplex of `Δ[r]` in a degree `q ≠ r` is non-surjective (hence in `∂Δ[r]`) or surjective and therefore degenerate, so `P∞` lands in the boundary chains, and the (now natural) normalization homotopy exhibits every relative cycle as a relative boundary → `isZero_simpRel_homology` (`H_q^simp = 0` for `q ≠ r`);
  * degree `r`: the relative chain module is spanned by the identity `r`-simplex, so every class is a multiple of the canonical generator `e_r`; combined with Task 20's `homologyMap_cellRelJ_top`, `stdCellGenerator_ne_zero`, `stdCellGenerator_eq_stdCellTopClass` and Task 19's one-dimensionality, this gives injectivity **and** surjectivity (`bijective_homologyMap_cellRelJ_top`) by the minimal ℤ₂ argument — no bases, dimensions or determinants;
  * non-top degrees: source zero (above) + target zero (Task 19 B1) ⇒ the unique morphism is an isomorphism, with no explicit inverse constructed;
  * endpoint `isIso_homologyMap_cellRelJ (r q : ℕ) : IsIso (HomologicalComplex.homologyMap (cellRelJ r) q)`, packaged as `homologyIso_cellRelJ` and as the optional `instance quasiIso_cellRelJ (r) : QuasiIso (cellRelJ r)`; also recorded: `isLine_simpRel_homology_top` and `simpTopClass_ne_zero`.
* `RequestProject/Spine/Nerve/Task21AxiomAudit.lean` — `#print axioms` for every principal declaration.

Scope respected: no attached-cell additivity, no skeletal decomposition, no B3/`RelJIsIso`, no `geometricHmap`, no nerve theorem, no `w₂(TM)`; these remain open and untouched.

Verification: `lake build RequestProject` → **Build completed successfully (8254 jobs)**, with the pre-existing firewall audit reporting all checks passed. Every Task-21 declaration reports exactly `[propext, Classical.choice, Quot.sound]`; no `sorry`, `admit`, new `axiom` or `native_decide`.

Documentation: `TASK21_AUDIT.md` (all 14 required points, including the exact `cellRelJ` used, the reused and newly required lemmas, the two proof routes, and confirmation that additivity and B3 remain open) and `TASK21_FAILBUILDS.md` (nine failed attempts with error, diagnosis, repair, and confirmation that no mathematical statement changed). `ARISTOTLE_SUMMARY.md` was not edited.

# Summary of changes for run bf5ff893-c9e6-4a39-86ef-7730eadb3d76
**Task 20 — Outcome A: the canonical standard-cell generator is identified.** All required theorems are proved, sorry-free, at the frozen pin (Lean v4.28.0, Mathlib rev 8f9d9cff…; toolchain, lakefile and manifest untouched).

Proved (all in namespace `SpineTask20`, in `RequestProject/Spine/Nerve/Task20CanonicalGenerator.lean`):

* `hcls_bdFaceChain_eq_bdFundClass (m)` — the explicit face-sum cycle ∑ᵢ ∂ᵢΔ^{m+1} represents the distinguished boundary fundamental class `bdFundClass (m+1)`. This is the principal compatibility theorem (Task 19's recorded missing result), together with the detection lemma `hcls_bdFaceChain_ne_zero`.
* `pairDelta_stdCellGenerator (k)` — the connecting homomorphism of the pair sends `stdCellGenerator (k+1)` to `bdFundClass (k+1)`.
* `stdCellGenerator_ne_zero (r)` — for every `r`.
* `stdCellGenerator_eq_stdCellTopClass (r)` — `γ_r = stdCellTopClass r` for every `r`.
* Optional §6 corollary, obtained: `homologyMap_cellRelJ_top (r)` — the single-cell relative comparison sends the simplicial top generator to `γ_r` (no isomorphism claim).

Route used: A/B hybrid. The face-sum cycle in |∂Δ[n+2]| is split into the part carried by the faces i ≠ 0 (inside the open set `Uset`) and the face 0 (inside `Vset`); the boundary of the U-part is the image of the lower-dimensional face-sum cycle under the 0-th face inclusion, which admits an honest continuous retraction and is therefore injective on homology, so the relative class is nonzero, and the already existing Task-18 excision isomorphism transports the nonvanishing back. Equality with `bdFundClass` then follows from the frozen one-dimensionality statement. No new Mayer–Vietoris machinery, no general horn or cellular homology, no new excision theory; Route C was never needed.

Supporting modules added: `Task20Coords.lean` (coordinate geometry and the radial retraction), `Task20FaceMaps.lean` (codimension-one face maps and injectivity of induced chain maps), `Task20Cover.lean` (the two-set open cover and the retraction), plus `Task20AxiomAudit.lean`.

Frozen Task-19 declarations were reused unchanged; nothing was redefined or replaced. B3 (and `RelJIsIso`, `geometricHmap`, the nerve theorem, w₂ transport) remains open and untouched.

Verification: `lake build RequestProject` → **Build completed successfully (8251 jobs)**, with the spine firewall audit reporting all checks passed. `#print axioms` for every Task-20 declaration — including `pairDelta_stdCellGenerator`, `stdCellGenerator_ne_zero`, `stdCellGenerator_eq_stdCellTopClass` and `hcls_bdFaceChain_eq_bdFundClass` — reports exactly `[propext, Classical.choice, Quot.sound]`; no `sorryAx`, no `sorry`/`admit`/new `axiom`/`native_decide`.

Documentation: `TASK20_AUDIT.md` (all 11 required points, outcome classification) and `TASK20_FAILBUILDS.md` (every failed attempt with error, diagnosis, repair, and confirmation that no mathematical statement changed). `ARISTOTLE_SUMMARY.md` was not edited.

# Summary of changes for run 1d23c739-9264-466f-834e-f83d44f01856
## Task 17 — outcome **D**: boundary topology closed, homology calculation still open

Everything below is machine-checked in the pinned environment: `lake build RequestProject` completes successfully (8223 jobs, including the pre-existing firewall audit), with **no `sorry`, no `admit`, no new axiom**; all 22 principal new declarations report exactly `[propext, Classical.choice, Quot.sound]` (`RequestProject/Spine/Nerve/Task17AxiomAudit.lean`). No existing module, the toolchain, the lakefile or the manifest was modified, and `J = C_*(η_K)` and `J_r^rel` were never replaced.

### New modules
* `RequestProject/Spine/Nerve/Task17StandardCellTopology.lean` — WP2, WP3, WP5, low-dimensional WP6
* `RequestProject/Spine/Nerve/Task17BoundarySphere.lean` — WP6 (the sphere)
* `RequestProject/Spine/Nerve/Task17AxiomAudit.lean`
* `TASK17_AUDIT.md` (WP1 inventory, WP4 route audit, dependency DAG, the 18-point final audit), `TASK17_FAILBUILDS.md` (13 failbuilds with provenance, diagnosis, repair)

### What is now theorem-level
* **WP3** `simplexHomeo` — `|Δ[n]|` is *homeomorphic* to the geometric standard simplex, and its underlying map is **definitionally** the Task-16 coordinate bijection (`coe_simplexHomeo`, by `rfl`), with naturality and `simplexHomeo_vertex` (vertices ↦ barycentric vertices). Compactness and Hausdorffness of `|Δ[n]|` are proved, not assumed.
* **WP5** `contractibleSpace_realized_simplex` — `|Δ[n]|` is contractible (convexity transported through the exact realization model).
* **WP2** `compactSpace_realized_subcomplex` — the realization of **every** subcomplex `A ≤ Δ[r]` is compact (finite cover by realized `r`-simplices of `A`, from the Task-16 normal form); hence `isClosedEmbedding_realization_subcomplex` — the realized inclusion is a **closed topological embedding**, so `|A|` carries the subspace topology.
* **WP2, boxed target** `boundaryHomeo` — `|∂Δ[r]| ≃ₜ {x : ∃ i, λᵢ(x) = 0}`: the Task-16 *set-theoretic* image identification is upgraded to a **homeomorphism onto the barycentric boundary locus**, plus openness of the relative interior.
* **WP6** `boundaryRealizationSphereHomeo` — `|∂Δ[r]| ≃ₜ S^{r-1}` (`Metric.sphere (0 : EuclideanSpace ℝ (Fin r)) 1`), by explicit radial projection from the barycentre, uniformly in `r`; low dimensions handled openly: `|∂Δ[0]|` empty, `|∂Δ[1]| ≃ₜ` discrete two-point space (`S⁰`).

### What is *not* claimed
B1 (relative homology of the standard cell pair) is **not** proved in any degree, its canonical generator is not identified, the single-cell relative `J` is not shown to be an isomorphism, no excision/additivity theorem was proved, B3 is **not** closed (`RelJIsIso` remains a hypothesis), finite-dimensional `J` is still conditional, and `geometricHmap` remains open. B1 was not weakened into a structure and was not identified with B3.

### Exact first remaining blocker
Audit of the pin: `Mathlib/AlgebraicTopology/SingularHomology/Basic.lean` contains only the definition of singular homology plus one computation; the string `excision` occurs nowhere in Mathlib, Mayer–Vietoris exists only for sheaves, and there is no barycentric subdivision, no LES of a pair, no homology of spheres. So on the now-shortest route (contractibility + LES + `|∂Δ[r]| ≅ S^{r-1}`, all three of which are in place) the first missing statements are: **(M1)** chain-level homotopy invariance for the project's ℤ₂ singular chain complex (the chain analogue of the project's existing cochain prism, a bounded construction), and behind it the true obstruction **(M2)** excision / Mayer–Vietoris / small simplices for singular homology — the same input that WP11 would need, which is a further reason to keep B1 and B3 distinct.


# Summary of changes for run afeca040-8e5b-4780-a967-98d2c81ce1fb
## Task 16 — outcome **B** (full point normal form + standard-cell theorem), with the boxed target proved

The blocker carried since Task 14/15 is closed. Everything is machine-checked in the pinned environment: full build green (8220 jobs), no `sorry`, no `admit`, no new axioms; every principal new declaration reports exactly `[propext, Classical.choice, Quot.sound]`. The toolchain, lakefile and manifest are untouched, no existing module was modified, and the canonical comparison `J = C_*(η_K)` was never replaced.

### The required result

**`|∂Δ[r]| → |Δ[r]|` is injective for every `r`** — `SpineTask16.standardCellMono`, stated for the project's frozen `SpineTask15.StandardCellMono`, i.e. for the actual boundary inclusion and the actual realization functor `SSet.toTop`.

Consuming the Task-15 reduction (not reproving it) then gives, unconditionally:

**`|K^(r-1)| ↪ |K^(r)|` for every simplicial set `K` and every `r`** — `SpineTask16.realization_skInc_injective`.

### How the realization was opened up (no new model)

`SSet.toTop` is a left Kan extension that the library never evaluates. Instead of substituting a model, the points were reached through the library's own data:

* **Level A** — `exists_representative`: every point of `|A|` is `|f| t` for some `f : Δ[n] ⟶ A`, obtained from the tautological colimit-of-representables presentation, colimit preservation by the realization (a left adjoint), and joint surjectivity of colimit cocones of types.
* **Coordinates** — `coord` / `coord_naturality`: `|Δ[n]| ≃ stdSimplex ℝ (Fin (n+1))`, *defined from the library identification* `SSet.toTopSimplex`, so it is automatically compatible with characteristic simplices.
* **Normal form (Level C)** — `subNormalForm` / `normalForm`: every point of `|A|`, for any subcomplex `A ≤ Δ[r]`, is the image of an *interior* point of a realized nondegenerate face; degeneracies are removed using the splitting of epimorphisms in the simplex category, zero coordinates using support restriction. Uniqueness of the pair (face, interior point) is what yields injectivity.
* Only the surjectivity half of the colimit presentation is used — all identifications are functoriality — which is why the absence of a quotient model in the library was not fatal.

### Also proved

* `subcomplexMono` — realization of the inclusion of **any** subcomplex of `Δ[r]` is injective (strictly stronger than the boundary case).
* `range_bdCoord`, `realized_boundary_eq_iUnion_faces`, `compl_range_bdCoord`, `range_realization_boundary` — the realized boundary is exactly `{λ : ∃ i, λᵢ = 0}`, the union of the `r+1` coordinate faces; its complement is the relative interior. Derived from the actual simplicial definition of `∂Δ[r]`, not assumed.
* `oneSkeletonStep`, `skeletalInduction`, `finiteDimensional_homologyIso` — the earlier skeletal machinery, now carrying only the relative-comparison hypothesis `RelJIsIso`.
* Smoke tests: `|∂Δ[0]|` is empty; every point of `|∂Δ[1]|` is one of the two vertices.

### Not proved (explicitly, per scope)

Realization of an arbitrary simplicial monomorphism (would need the Eilenberg–Zilber lemma plus a normal form for the target); topological/closed embedding (needs the topology of `|A|`, not just its points); and, as instructed, nothing about `H_q(|Δ[r]|, |∂Δ[r]|)`, excision or subdivision.

### New files

`RequestProject/Spine/Nerve/Task16PointModel.lean`, `Task16Consequences.lean`, `Task16AxiomAudit.lean`; documentation in `TASK16_AUDIT.md` (16-point report, exact source-level dependency map, dependency DAG with every arrow proved) and `TASK16_FAILBUILDS.md` (nine failed builds with diagnosis and repair, plus rejected routes); `DEPENDENCY_DAG.md` has a Task-16 addendum.

### Next blocker

Every arrow of the required DAG is proved, so the next problem is the one named in the task: `H_q^sing(|Δ[r]|, |∂Δ[r]|; ℤ₂)` together with its canonical top generator (project-internally, `RelJIsIso`). Its three precise missing inputs are listed in `TASK16_AUDIT.md` §16: upgrading the boundary bijection to a homeomorphism onto `{λ : ∃ i, λᵢ = 0}`, the homology of the disk/sphere pair, and singular excision — none of which exist in the pinned library.

# Summary of changes for run ff960f86-5be8-4711-9ee5-a4293ec0f3f3
## Task 15 — outcome `F` (precise remaining blocker), with substantially more proved than in Task 14

All work is machine-checked in the pinned environment. Three new modules, no `sorry`, no `admit`, no new axioms; every new principal declaration reports exactly `[propext, Classical.choice, Quot.sound]`. Full `lake build` green (8217 jobs); firewall audit unchanged (`all checks passed`, external-project imports = 0). The toolchain, lakefile and manifest are untouched, and the canonical comparison `J = C_*(η_K)` was never replaced — every new theorem is stated for it.

### WP1 — closed in full (`RequestProject/Spine/Nerve/Task15Pushout.lean`)

The Task-14 gap was that the concrete skeletal attachment existed only as a bespoke universal property, never as a `CategoryTheory.IsPushout`, so the generic transport theorem could not be applied to it. Now proved:

* `skeletalIsPushout` — the concrete square `∐_{σ∈NDeg_r} ∂Δ[r] → K^(r-1)`, `∐_σ Δ[r] → K^(r)` **is** a categorical pushout of simplicial sets, derived from the Task-14 universal property (`attachExtend`, `…_comp_skInc`, `…_cell`, `…_unique`). The attaching maps remain the canonical `σ·(−)`.
* `skeletalIsPushout_toTop` — the theorem-level composition with the Task-14 generic result: the realized square is a pushout of spaces.
* `skeletalIsPushout_toTop_coprod` — the same square written explicitly with `∐_σ |∂Δ[r]|` and `∐_σ |Δ[r]|`.

### WP2/WP3 — blocker B2 sharply reduced, and closed at r = 0 (`Task15RealizationMono.lean`)

The audit found that `SSet.toTop` occurs in exactly one file of the pinned library: there is no theorem on injectivity, embeddings, closed embeddings, cofibrations, preservation of monomorphisms or limits, and no pointwise model of `|X|`. Instead of assuming injectivity, it is reduced:

* `realization_skInc_injective` — **if `|∂Δ[r]| → |Δ[r]|` is injective, then `|K^(r-1)| → |K^(r)|` is injective for every simplicial set `K`.** Proof: the WP1 pushout is carried into `Set` by the colimit-preserving realization-and-forget functor; the left vertical map is a coproduct of injections; `Set` is adhesive, so the pushout of a monomorphism is a monomorphism.
* `standardCellMono_zero` — the `r = 0` case is proved outright (`∂Δ[0]` is the initial simplicial set), hence `realizationInjective_zero` is unconditional.
* `oneSkeletonStep_of_standardCell` — the Task-14 step with the arbitrary-`K` hypothesis replaced by the single standard-cell statement.

### WP12 — base case closed, induction assembled (`Task15BaseCase.lean`)

* `baseCase` — **unconditional**: `J` is a homology isomorphism on the empty bottom skeleton (both chain complexes are zero).
* `skeletalInduction`, `finiteDimensional_homologyIso` — for every finite-dimensional simplicial set, `J = C_*(η_K)` induces homology isomorphisms in all degrees, conditional on exactly two explicitly carried hypotheses: injectivity of `|∂Δ[r]| → |Δ[r]|`, and the relative comparison isomorphism at each level.

### What is *not* proved

The standard-cell homology theorem with its generator (WP5/WP7), the excision-strength cell decomposition (WP9), `H(J^rel)` iso (WP11), and the cohomology bridge (WP13). `geometricHmap` is **not** proved bijective, and no claim to that effect is made anywhere.

### Sharpened diagnosis and the required documentation correction

The audit (`TASK15_AUDIT.md`) shows that Routes A, B and C for the standard-cell pair all fail at the *same* first step, and it is not excision: the pin never evaluates the left Kan extension defining geometric realization outside the representables, so `|∂Δ[r]|` cannot be identified with anything. That same gap is what blocks the remaining half of B2. Excision-strength small-simplices infrastructure is the *second* obstruction and is likewise entirely absent (itemised inventories for WP2, WP6 and WP8 are in the audit).

Accordingly the Task-14 label "full excision is genuinely required" is **withdrawn** and replaced by "no audited specialized route bypasses excision-strength small-simplices infrastructure in the pinned environment", recorded in an append-only correction section of `TASK14_SPECIALIZED_CELL_ATTACHMENT.md`; Task-14's status is restated there as: concrete simplicial attachment proved, generic realization of pushouts proved, their theorem-level composition done here in WP1.

### Documents

`TASK15_AUDIT.md` (WP-by-WP status, the three inventories, the dependency DAG, reused and new modules, build/axiom audit, failed routes, and explicit answers to all thirteen required final questions), `TASK15_FAILBUILDS.md` (eight entries with provenance, diagnosis, repair and classification — none required a statement change), plus addenda to `DEPENDENCY_DAG.md` and `README.md`. `ARISTOTLE_SUMMARY.md` was not modified.


# Summary of changes for run f86b9ecb-19e7-4451-8235-680cffbfffc0
## Task 14 — specialized cell attachment and the relative singular bridge

**Hard outcome (WP10): `B — FULL EXCISION IS GENUINELY REQUIRED`**, in the precise sense that the *small-simplices / barycentric-subdivision* theorem is unavoidable on every route audited (excision, Mayer–Vietoris, the good-pair quotient theorem and `H_*(Sⁿ)` are all downstream of it). A **second, logically independent blocker** was also found: *geometric realization of a monomorphism of simplicial sets is injective*. The earlier audit's instruction not to collapse the two missing components into "excision is missing" is vindicated.

But the simplicial and algebraic halves are now **complete and machine-checked**.

### Proved (all `sorry`-free; every principal declaration reports exactly `propext, Classical.choice, Quot.sound`)

* **WP3 — skeletal cell attachment** (`Task14SkeletalAttachment.lean`). The pinned library lists this as an open TODO. Proved: the degreewise decomposition `(K^(r))_n ≅ (K^(r−1))_n ⊔ ∐_{σ ∈ NDeg_r} {surjections ⦋n⦌ ↠ ⦋r⦌}` — i.e. the pushout `K^(r) = K^(r−1) ∪_{∐∂Δ[r]} ∐Δ[r]` evaluated degreewise — plus the full universal property (`attachExtend`, restriction, cell values, uniqueness). Attaching maps are the canonical `σ·(−)`; nothing is chosen.
* **WP4 — realized pushout**. The concrete instance that realization preserves colimits, and `toTop_isPushout`. `|K^(r)|` is the topological pushout obtained by attaching realized standard simplices. No CW structure is claimed.
* **WP7 — minimal relative chain complex** (`Task14RelativeChains.lean`): `C_*(T,S) = C_*(T)/im C_*(S)`, well-defined differential, quotient chain map, functoriality for maps of pairs, and the short exact sequence `0 → C_*(S) → C_*(T) → C_*(T,S) → 0` for any degreewise injective map — specialised to topological pairs and to simplicial subcomplex pairs.
* **WP8 — pair long exact sequence** with naturality of the connecting map.
* **New: the middle five lemma** `τ₂` for ℕ-indexed chain complexes (the pinned library proves only the `τ₃` version and lists this as a TODO).
* **WP12 — canonical relative comparison** `relJ`, induced by the frozen `J = C_*(η_K)` via the naturality square of the adjunction unit (`relJ_tau₂` is `rfl`), with the **generator identification**: `J^rel[σ]` is the characteristic singular simplex of `σ`.
* **WP13 — one-skeleton step**: `realization injective` ∧ `H(J|K^(r−1))` iso ∧ `H(J^rel)` iso ⟹ `H(J|K^(r))` iso — a genuine Lean theorem via the pair LES, naturality and the new five lemma.
* **WP17 — controls**: agreement with Nakahara Example 3.9 (filled triangle) and Example 3.8 (circle); the tetrahedron control (Exercise 3.3) was **not performed** and is recorded as such.

### Blockers, stated as Lean propositions (not axioms, not `sorry`)

`StandardCellPairAcyclic` / `StandardCellPairTop` (B1), `RealizationInjective` (B2), `RelJIsIso` (B3) — used only as explicit hypotheses. Task 14 reduces entirely to these three.

### Documents

`TASK14_SPECIALIZED_CELL_ATTACHMENT.md` (pinned-infrastructure audit, WP5/WP6 route audits, dependency DAG, the hard decision, the three blockers with exact statements/Lean types/proof architectures, endpoint classification, and precise answers to every Final Question), `TASK14_TOPOLOGY_SOURCES.md` (Nakahara Chapter-3 ledger with PDF pages, and the separate non-Nakahara sources — the book contains no relative homology, no excision, no CW theory, no `H_*(Sⁿ)`), `TASK14_FAILBUILDS.md` (seven failbuilds, all classified `NONE` except one `PRESENTATION`), and a Task-14 section appended to `DEPENDENCY_DAG.md`.

### Recommended Task 15

Barycentric subdivision of singular chains with its chain homotopy → the small-simplices theorem → excision; and, independently and in parallel, injectivity of the realization of a monomorphism. Then B1, B3 follow and `oneSkeletonStep` becomes unconditional.

### Verification

All WP21 targets build, plus the six new Task-14 modules and a full `lake build`. Firewall unchanged: Experiment1 direct/transitive = 0, Experiment2 direct/transitive = 0, external-project imports = 0. No `sorry`, `axiom`, `unsafe`, `partial`, `implemented_by` or `native_decide` in the Task-14 layer. Nothing from Tasks 9–13 was modified.


# Summary of changes for run 121809a3-e234-423b-b5ab-b4fcb7903ed8
## Task 13 — normalized skeletal comparison and cohomology bridge

Everything below is machine-checked in the pinned environment (Lean v4.28.0, Mathlib as pinned in `lake-manifest.json`); the four new modules are `sorry`-free and contain no `axiom`, `unsafe`, `partial` or `implemented_by`.

### New Lean layer (all proved)

**Objective A — normalization of the *actual* project complex** (`RequestProject/Spine/Nerve/Task13Normalization.lean`)
* `degenSubmodule` — the degeneracy submodule `D_q ⊆ C_q^{simp,unn}(S;ℤ₂)`; `boundary_degen_mem` — `∂D_{q+1} ⊆ D_q`, so `D` is a subcomplex.
* `NormChain`, `normBoundary`, `normProj` (`p`), `normInc` (`i`), `normHtpy` (`h`) with `normProj_comp_normInc` (`p∘i = id` on the nose), `normInc_comp_normProj` (`i∘p = P∞`) and `normHtpy_zero`/`normHtpy_succ` (`∂h + h∂ = id + i∘p`): the unnormalized complex is **chain-homotopy equivalent** (not just quasi-isomorphic) to the normalized quotient, for an arbitrary simplicial set.
* Naturality: `normMap`, `normProj_natural`, `normInc_natural`, `normMap_comm`, `sSetChainMap_degen_mem`.
* Bridge to the pinned library: `afmc_d_hom` proves the project's own boundary **is** Mathlib's alternating-face-map differential (signs vanish in characteristic 2), so the pinned Dold–Kan projector and its homotopy act directly on the project's complex — no parallel theory was created.

**Objective B — what normalization computes, and the skeletal pair** (`Task13Skeletal.lean`)
* `degenSubmodule_eq_supported` — `D_q` is *exactly* the chains supported on degenerate simplices; `normChainEquiv` — `N_q` is free over ℤ₂ on the **nondegenerate** `q`-simplices.
* `relNormChain_subsingleton_of_lt` / `_of_gt` — `N_q(K^{(r)},K^{(r-1)}) = 0` for every `q ≠ r`; `relNormChainEquiv` + `nonDegenerateSkeletonEquiv` — degree `r` is free on the nondegenerate `r`-simplices.
* `relNormProj`, `relNormInc`, `relNormProj_comp_relNormInc` — the relative normalization comparison, proved to be a split surjection.

**WP1/WP12 freeze** (`Task13CanonicalMap.lean`) — `chainMap_degen_mem`, `normJ` (= the natural construction applied to the adjunction unit itself), `normJ_normProj` (`p∘J = normJ∘p`), `normJ_comm`. The canonical `J = C_*(η_K)` was never replaced.

**Pinned-library probes and axiom audit** (`Task13PinAudit.lean`) — every principal Task-13 declaration reports exactly `[propext, Classical.choice, Quot.sound]`.

### Audit outcome (documents: `TASK13_NORMALIZED_SKELETAL_COMPARISON.md`, `TASK13_FAILBUILDS.md`, `TASK13_EXTERNAL_SOURCES.md`, README section)

* Normalization: **chain-homotopy equivalence**, natural, arbitrary simplicial set.
* Relative normalization: split surjection proved; the complementary relative homotopy is deferred (it needs naturality of the pinned contracting homotopy, which the library states only for its building blocks).
* Singular side: the pinned library has **no relative singular homology and no excision** (0 declarations), no relative homology of the standard cell pair, no CW structure on realizations and no cellular homology — so the one-step skeletal comparison and the skeletal induction are **blocked**; the first missing theorem is excision, with its dependency cone written out.
* Bridge decision: **A** (quasi-iso ⇒ chain-homotopy equivalence ⇒ `geometricHmap` bijective). Its algebraic half is not in the pin as a statement but is derivable from the pinned K-projective machinery (probes compile); its second half is already proved in the project (Task 10). A direct cohomological route needs the same missing topology plus a dualisation, so it is strictly more expensive.
* Scope reached: the simplicial/algebraic side of one skeleton step for arbitrary `K`. No claim of `H_*^{simp}(K) ≅ H_*^{sing}(|K|)` at any scope; `geometricHmap` is **not** unconditionally bijective and the conditional `GeometricComparison` assumption stays in place.

### Verification
`lake build` for `Spine.Nerve.Core`, `Spine.Cohomology.Core`, `Spine.Cech.Core`, `Spine.GoodCover.Core`, `Spine.Geometry.Core`, `Spine.Core`, `Spine.Audit.Firewall`, plus the four new modules and a full `lake build`: all succeed. Firewall output unchanged — Experiment1 direct/transitive = 0, Experiment2 direct/transitive = 0, external-project imports = 0.


# Summary of changes for run ff9c36d7-4914-4945-acfe-9e5b83c3827f
## Task 12 — Acyclic-Models applicability audit: decision **B (FORWARD ONLY — REVERSE ROUTE REQUIRED)**

The applicability question is settled, and settled **negatively for the direction that was needed**. Everything decisive is proved in Lean, not merely argued.

### The verdict, in one line
For `F(K) = C_*^simp(K;ℤ₂)` and `G(K) = C_*^sing(|K|;ℤ₂)` on the project's own simplicial-set category, classical Acyclic Models applies **only in the direction `F → G`** — which reproduces exactly the map Task 10 already owns, `J = C_*(η_K)` — and **cannot construct the reverse map `R : G → F`**. Endpoint: `ONLY_FORWARD_MAP_F_TO_G`.

### New Lean layer (`RequestProject/Spine/Nerve/Task12Probe.lean`), all `sorry`-free
* `freeOnStdSimplex_bijective`, `simpChain_model_span`, `simpChain_model_generator` — **`F` is free on the standard-simplex models** (Yoneda), with the generators in exactly the form the theorem requires.
* `stdSimplex_chain_acyclic` (via `cone_chain_homotopy`) — **`F` is acyclic on those models**, by an explicit formalised cone contraction `∂h + h∂ = id`.
* `realization_stdSimplex_contractible`, `singularCohomology_stdSimplex_vanishing` — `|Δ[n]|` is contractible and its positive-degree singular mod-2 cohomology vanishes (**`G` acyclic on the models**), reusing the project's own homotopy invariance.
* `singularGenerator_not_surjective`, `singularGenerator_not_spanning` — **`G` is *not* free on those models**: for `K = Δ[1]` there are finitely many simplicial maps `Δ[q] ⟶ K` but uncountably many singular simplices of `|K|`.
* `no_countable_family_generates`, `hom_stdSimplex_ext`, `finite_hom_to_stdSimplex`, `countableFiniteModels_not_generating` — a uniform obstruction that rejects **every countable model class whose models have finitely many vertices** (standard simplices, finite simplicial sets, contractible finite ones, boundaries, subdivisions).
* `boundary2_homology_one_ne_zero` with `circleModel_isFinite` — **finite simplicial sets are not an acyclic model class**: `∂Δ[2]` (finite in the library's own sense, realisation a circle) carries a mod-2 `1`-cycle that is not a boundary, certified by the "nondegenerate-edge count mod 2" functional that kills all boundaries.
* `generator_eq_unit_comp` — the adjunction repairs nothing: the model generators are exactly the singular simplices factoring through `η_K`, so the argument would be circular.
* `acyclicModels_forward_only` — the verdict as a single theorem; plus compile probes for the successor route (`SSet.skeleton`).

All 47 new declarations report axioms `[propext, Classical.choice, Quot.sound]`; no `sorry`, `admit`, project-local `axiom`, `unsafe`, `partial` or `implemented_by`.

### Documentation
`TASK12_ACYCLIC_MODELS_APPLICABILITY.md` covers WP1–WP18: the exact theorem statement with its **asymmetry made explicit** (freeness on the *domain*, acyclicity on the *codomain*), the instantiated hypothesis tables, both model-class audits, the compact-support audit (true classically, absent at the pin, and *route-specific* — finite-subcomplex factorisation is not freeness on models), the alternative-class audit including `{Sing|Δ^n|}` (rejected via a representability argument), the circularity record, the classical-proof architecture audit (Hatcher §2.1 Thm. 2.27, read verbatim: skeleta + long exact sequences + `(Δ^r,∂Δ^r)` + five lemma + compact support — **no acyclic models**), the three-route comparison, both required decision matrices, the hard decision gate, endpoint classification, and answers to every final question. Supporting files: `TASK12_EXTERNAL_SOURCES.md` (full ledger with identifiers, access dates, influence classes; nothing imported) and the append-only `TASK12_FAILBUILDS.md` (eight entries, every repair proof-only). `DEPENDENCY_DAG.md`, `PROVENANCE_LEDGER.md` and `README.md` received append-only addenda.

### Task-13 recommendation
**Route C — skeletal/cellular comparison**, with Route K's compact-support step as its final layer; subdivision/simplicial approximation is strictly larger, and compact support alone is only a reduction. The successor DAG (L0 owned → relative theories → long exact sequence of a pair → `(Δ^r,∂Δ^r)` and excision → five lemma/induction → colimit → assembly) is in the audit and in `DEPENDENCY_DAG.md`. The Task-10 map `J = C_*(η_K)` is untouched and is now additionally protected: on these models the uniqueness clause says any natural augmentation-preserving comparison `F → G` is naturally chain homotopic to `J`.

### Verification
`lake build` green (8203 jobs); the five requested module builds all green; firewall `all checks passed`; `Experiment1`/`Experiment2` direct and transitive imports = 0 and external-project imports = 0; toolchain and manifest byte-identical to their previous state. The chain-level comparison itself remains unproved — that status is unchanged and honestly recorded.


# Summary of changes for run b40aca65-3d71-45a3-a0a7-0dbd1ddcf684
## Task 11 — Upstream Delta Audit: **Outcome C, NO MATERIAL SHORTENING**

The production environment was frozen: `lean-toolchain`, `lakefile.toml` and `lake-manifest.json` are byte-identical to their Task-10 state, and current upstream Mathlib was treated purely as an external research source (separate scratch checkout, never linked into the Lake project).

### Deliverables added
* `TASK11_UPSTREAM_DELTA_AUDIT.md` — the full audit (WP1–WP18): provenance, module delta, direct-theorem search, Quillen status, relative-homology and Eilenberg–Steenrod deltas, Dold–Kan, PR/issue history, backport and upgrade feasibility, decision matrix, hard decision gate, build evidence, axiom audit.
* `TASK11_EXTERNAL_SOURCES.md` — external source ledger (nothing imported; firewall unchanged).
* `RequestProject/Spine/Nerve/Task11Probe.lean` — five compile probes (`unitChainComplexMap`, `unitChainComplexMap_target`, `unitChainComplexMap_naturality`, `UnitQuasiIsoStatement`, `taskNine_and_mathlib_share_the_unit`), all `sorry`-free, axioms `[propext, Classical.choice, Quot.sound]`.
* Corrections: `ComparisonSpec.lean` and `ChainHomotopyComparison.lean` docstrings, an append-only `TASK 11 CORRECTION SECTION` in `TASK10_AUDIT.md`, plus addenda to `PROVENANCE_LEDGER.md` and `DEPENDENCY_DAG.md`.

### Status corrections carried forward
Chain-homotopy-equivalence data is now stated as a **stronger**, more structured *sufficient* condition (`ChainHomotopyEquivData ⟹ GeometricComparison ⟹ geometricHmap bijective`, no converse), not a weaker assumption; and the compact-support/finite-subcomplex theorem is labelled **route-specific** to the Acyclic-Models route, not the unique logical blocker. The route-independent target remains bijectivity of `geometricHmap`.

### Final answers
* **Audited upstream commit:** `076c9da2981330e0d1ba84a10afa6544faafa612` (branch `master`, 2026-09-09, toolchain `v4.34.0-rc2`), against the pin `8f9d9cff6bd728b17a24e163c9402775d9e6a365` (tag `v4.28.0`, 2026-02-16) — 6002 commits apart.
* **Theorem that η_K induces a homology/cohomology isomorphism?** No. Upstream names the map (`SSet.chainComplexMap (sSetTopAdj.unit.app K) R`, `SSet.homologyMap …`) but proves nothing beyond naturality; a library-wide `exact?` on `IsIso (SSet.homologyMap (sSetTopAdj.unit.app K) R n)` fails, and `QuasiIso` has no instance.
* **Quillen equivalence?** No — `SingularSet.lean` still lists "Sing X is a Kan complex" and "`sSetTopAdj` is a Quillen equivalence" as TODOs, verbatim as in the pin; there is still no Kan–Quillen model structure.
* **New relative-homology / Eilenberg–Steenrod infrastructure:** `SSetPair` with relative chain complex, `homologyδ` and the long exact sequence (PR #41285); `TopPair` (PR #36621) and `TopPair.HomologyPretheory` with only the homotopy-invariance class (PR #39236) — **no uniqueness theorem**. Also new: `SSet.homology`, homotopy invariance on both sides, `H₀` computations, the normalized chain complex, and the `sd ⊣ ex` subdivision adjunction (definitions only).
* **Does any of it shorten the actual `geometricHmap` proof?** No — no dependency chain reaches `C_*(η_K)`; excision, acyclic models, homology of `Δ[n]`/contractible spaces, simplicial approximation and compact support are all still absent upstream.
* **Dold–Kan:** still `UNCHANGED_NORMALIZATION_ONLY`.
* **Shortest credible native route:** Acyclic Models over `SSet` with models `Δ[n]`, mod 2, producing the chain-homotopy-equivalence datum that Task 10 already converts into bijectivity.
* **First route-specific missing theorem:** compact supports — every singular simplex of `|S|` factors through the realisation of a finite subcomplex (resting on the colimit description of `|S|`).
* **Backport vs upgrade:** backporting the useful delta is strictly smaller (`MEDIUM_INFRASTRUCTURE_DELTA`) than a full upgrade (`HIGH_RISK`), but neither is recommended, since neither supplies the missing theorem.
* **Decision: C — NO MATERIAL SHORTENING.** **Task-12 recommendation:** keep the pin, no backport, no upgrade; scope Task 12 to the colimit description of `|S|` over finite subcomplexes plus the compact-support theorem, deferring acyclicity of the models, freeness, the acyclic-models theorem and assembly to later tasks.

### Verification
`lake build` and each named target build green (full build 8202 jobs, +1 over Task 10 for the probe module); firewall reports `external-project imports: 0`, `Experiment1 = 0`, `Experiment2 = 0` (direct and transitive), "all checks passed"; no `sorry`, `admit`, project-local `axiom`, `unsafe`, `partial` or `implemented_by` was added.


# Summary of changes for run 99bf037d-d5c1-471b-b00b-656ad74a2bf3
## Task 10 — outcome

**The comparison theorem is not proved unconditionally: this is a negative result with a substantially reduced blocker.** `NerveGeom.geometricHmap 𝓤 n` is *not* shown bijective outright. What is new is that the gap is now a single chain-level statement, and everything cohomological around it is proved.

Four new Lean modules under `RequestProject/Spine/Nerve/`, plus documentation. `lake build` is green (8201 jobs), the mechanical firewall reports `all checks passed` with `Experiment1/Experiment2/external-project imports = 0`, and all 68 `#print axioms` checks in `RequestProject/Spine/Nerve/Core.lean` report only `[propext, Classical.choice, Quot.sound]`. No `sorry`, `admit`, project-local `axiom`, `unsafe`, `partial` or `implemented_by`.

### Proved

* **WP0 (`ManifoldAssumptionAudit.lean`)** — the Task-9 claim that the Task-4 manifold layer supplies second countability/paracompactness is withdrawn *and refuted*: `SpineTask10.chartedNotSecondCountable` exhibits a space charted on the Task-4 carrier `ℝ × (Fin 3 → ℝ)`, and a `C^∞` manifold for the Task-4 model, that is not second countable; `SpineTask10.not_secondCountable_of_charted` kills the implication. Nothing was added to Task 4. Corrections applied in `ComparisonSpec.lean`, `TASK09_AUDIT.md` (append-only, originals struck through) and `DEPENDENCY_DAG.md`, with the required "required by the theorem" vs "currently derived" split.
* **WP2 (`UnitChainMap.lean`)** — `chainMap_eq_unitChainMap`: the Task-9 chain map **is** the free ℤ₂-linearisation of the adjunction unit `η : N(𝓤) ⟶ Sing|N(𝓤)|`, by `rfl`; both boundaries are the linearised boundaries (`simpBoundary_eq`, `singBoundary_eq`) and Task-9's `chainMap_comm` is naturality of `η`.
* **`Dualization.lean`** — explicit transpose `dualOf` of ℤ₂-linear maps of free modules, with `dualOf ∂ = δ` on both sides and `dualOf J = J*`. Exactness of dualisation is proved from the formula; no universal coefficient theorem anywhere.
* **WP5/WP7/WP10 (`ChainHomotopyComparison.lean`)** — `geometricHmap_bijective_of_chainHomotopyEquiv`: a chain-homotopy inverse for the *canonical* Task-9 chain map makes the *existing* `geometricHmap 𝓤 n` bijective in **every** degree (degree 0 handled separately, since `B⁰ = 0`; no finiteness assumed). Also `ChainHomotopyEquivData.toGeometricComparison`, so the Task-9 `GeometricComparison` is now derivable rather than assumable, and Spin-lift statements phrased on the chain datum alone.

### The remaining blocker

`NerveGeom.ChainComparisonStatement 𝓤` — "`J = C_*(η_K)` is a chain-homotopy equivalence". `TASK10_AUDIT.md` gives its exact statement, Lean carrier types, canonical map, closest pinned declarations, and the smallest additional theorem family required (compact supports in a realisation → natural chain contraction of the models → acyclic models → assembly).

### Answers to the final questions

1. **Is Task-9's `chainMap` the map induced by the unit?** Yes, definitionally.
2. **Does pinned Mathlib v4.28.0 imply it is a homology equivalence?** No — no `SSet.homology`, no comparison attached to `SSet.singularChainComplexFunctor`, no excision/Mayer–Vietoris/relative homology, no acyclic models, no subdivision, no Eilenberg–Steenrod file, no model structure on `SSet`/`TopCat`; Mathlib's own `SingularSet.lean` lists the Quillen equivalence as a TODO.
3. **What does Dold–Kan remove?** Only normalized-vs-unnormalized bookkeeping — and the project already uses unnormalized chains on both sides, so nothing. It leaves all topological content untouched: `NO_NET_SHORTENING`.
4. **A materially shorter route?** No. All seven audited routes are rejected; extra degeneracies/acyclic carriers is the recommended successor route but is a major independent project.
5. **Bijectivity without comparison data?** No — only conditionally on the chain-level datum.
6. **First missing theorem?** `ChainComparisonStatement`; its first missing prerequisite is that every singular simplex of `|S|` factors through the realisation of a finite subcomplex.
7. **Is degree 2 easier?** `NO_MEANINGFUL_REDUCTION` — `H²` already needs `C¹,C²,C³`, and the reduction proved here is uniform in the degree.
8. **Unconditional `[z]_{|N|}` after Task 10?** No; still conditional, but on the strictly weaker chain-level statement. The nerve theorem `|N(𝓤)| ≃ M` was deliberately left untouched and remains independent.

### Documentation
`TASK10_AUDIT.md` (capability audit, route matrix, negative-result statement, anti-shortcut compliance, verification evidence), `TASK10_EXTERNAL_SOURCES.md`, `TASK10_FAILBUILDS.md` (append-only, six entries, all repairs proof-only), plus updates to `DEPENDENCY_DAG.md`, `PROVENANCE_LEDGER.md`, `ARCHITECTURE.md`, `README.md`. No external project was imported.

# Summary of changes for run 689b97de-1b49-4c12-94da-12aea4f5e61f
Task 9 is delivered as a new Lean layer `RequestProject/Spine/Nerve/**` (6 modules + endpoint), plus `TASK09_AUDIT.md`, `TASK09_EXTERNAL_SOURCES.md`, and appended sections in `DEPENDENCY_DAG.md` and `ARCHITECTURE.md`. `lake build` is green (8197 jobs), the mechanical firewall reports "all checks passed", and the new layer contains no `sorry`, `admit`, project-local `axiom`, `unsafe`, `partial` or `implemented_by`. All 38 principal new declarations report only `[propext, Classical.choice, Quot.sound]`.

**Proved unconditionally**

- **WP1** — full audit of the existing nerve representation, with the exact dependency DAG and a per-component classification. The pre-Task-9 nerve had ordered vertices, simplex dimension and face maps, but **no degeneracies and no simplicial identities beyond `dᵢdⱼ`**; the `SSet`/realization/comparison items existed only as uninhabited specifications.
- **WP2** — `NerveGeom.coverNerveSSet` is a genuine simplicial set (an actual functor `Δᵒᵖ ⥤ Set`). The key point is that reindexing an index tuple along *any* simplex-category morphism preserves nonemptiness of the intersection, so degeneracies exist canonically: `sᵢ` repeats the `i`-th cover index. Faces are definitionally the existing Čech face operators. Every simplicial identity comes from the functorial structure and is restated concretely (`face_face_comm`, `degen_degen_comm`, the two collapse identities, and both mixed identities).
- **WP3** — the existing Čech complex used *all ordered tuples*, i.e. the **unnormalized** convention, so no normalization bridge is needed. `underlyingPresimplicial_coverNerveSSet` proves that forgetting the degeneracies returns *exactly* the existing presimplicial nerve, so cochains, coboundary, cocycles, coboundaries and cohomology are unchanged and `Ȟⁿ(𝓤;ℤ₂) ≅ Hⁿ_simp(N(𝓤);ℤ₂)` remains the identity. `isDegenerate_iff_exists_degen` proves the combinatorial and simplicial notions of degeneracy agree.
- **WP4** — audit of the realization infrastructure; the library's realization functor is reused, so `|N(𝓤)| = SSet.toTop.obj (coverNerveSSet 𝓤)` is a definition, not an interface field.
- **WP5** — `NerveGeom.charSimplex` (and `charMap`, as a continuous map out of the standard topological simplex) is the characteristic singular simplex, taken from the unit of the realization/singular adjunction. Face compatibility, degeneracy compatibility and the universal property (`charSimplex_universal`) are theorems; nothing is chosen or postulated.
- **WP6** — the chain map `J` and the theorem `∂_sing ∘ J = J ∘ ∂_simp`, whose proof consumes the WP5 face compatibility.
- **WP7** — the dual cochain map `J*` with `J*δ_sing = δ_simp J*`, proved to be the transpose of `J`, descending to `geometricHmap : Hⁿ_sing(|N(𝓤)|;ℤ₂) → Ȟⁿ(𝓤;ℤ₂)`. No cohomology-level map is introduced standalone.
- **WP12** — `canonicalNervePresentation` *constructs* the previously hypothesised nerve presentation, and `NerveTheoremData` has a single field `|N(𝓤)| ≃ X`, so the forbidden "arbitrary realization space" shape no longer occurs.

**Not proved (the negative result)**

- **WP8/WP9** — that `geometricHmap` is an isomorphism, i.e. `H*_simp(K;A) ≅ H*_sing(|K|;A)`. All three standard routes (skeletal/cellular induction, acyclic models, Dold–Kan plus one of the former) need singular-homology infrastructure absent from the pinned library: a CW/skeletal structure on the realization, excision/Mayer–Vietoris, or the acyclic-model theorem. A degree-2-only route is **not** materially smaller, and this is documented as a genuine limitation rather than a scope choice. The gap is recorded as `NerveGeom.GeometricComparison`, an explicitly conditional specification whose single field asserts bijectivity of the *constructed* canonical map — not an arbitrary linear equivalence, and consumed only by declarations documented as conditional.
- **WP10** — the Spin-lift class on the realized nerve (`spinLiftRealizedClass`) and the vanishing theorems `[z]_{|N|} = 0 ⟺ [z]_{Ȟ²} = 0 ⟺ coherent Spin lift exists`, and the frame form `⟺ a Spin structure exists`, are proved, but conditionally on that comparison. Unconditional transport is therefore not available; the class is not transported to the manifold.
- **WP14** — the nerve theorem, stated formally with hypotheses itemised (open cover, covering, paracompactness/numerability giving a subordinate partition of unity, contractible nonempty finite intersections; Hausdorffness not needed, local finiteness not assumed separately). In the existing manifold model openness, covering and paracompactness are available; contractibility of the finite intersections is not.

**Other deliverables** — WP11 supersession markers `SUPERSEDED_BY_GEOMETRIC_COMPARISON` on the five older comparison/realization interfaces (nothing deleted); WP13 reconstructed certification DAG; a complete failbuild ledger (three failures, each with command, diagnostic, cause, repair and statement-change classification — all were proof-term changes, no statement was weakened); full build evidence with `Experiment1`/`Experiment2` direct and transitive imports = 0 and external-project imports = 0; and the axiom audit. The source ledger records only pinned-library files, two of them as negative controls confirming the missing comparison theorem.

Answers to the final questions, in short: the nerve **is** now a genuine full simplicial set with canonical degeneracies; `|N(𝓤)|` **is** the actual realization of that simplicial set; the geometric chain/cochain comparison **has** been constructed from the canonical realization simplices; the induced degree-2 map is **not** proved to be an isomorphism (it is the one unreached target, recorded honestly); the Spin obstruction can be represented on `|N(𝓤)|` only conditionally; and **two** named theorems, not one, still separate the fixed good cover from `H²_sing(M;ℤ₂)` — the simplicial-to-singular comparison and the nerve theorem.

# Summary of changes for run aa531603-8d24-41af-b779-a24f93053a2c
## Task 8 delivered: native homotopy invariance of the singular mod-2 cohomology, plus comparison-interface hardening

`lake build` is green (8190 jobs), the mechanical firewall passes, and the six new modules contain no `sorry`, `admit`, project-local `axiom`, `unsafe`, `partial` or `implemented_by`. Every principal new declaration reports only `[propext, Classical.choice, Quot.sound]`.

### New Lean layer

```
RequestProject/Spine/Cohomology/CharTwo.lean            (WP4)
RequestProject/Spine/Cohomology/PrismMaps.lean          (WP3, combinatorial input)
RequestProject/Spine/Cohomology/Prism.lean              (WP3, WP4)
RequestProject/Spine/Cohomology/HomotopyInvariance.lean (WP5–WP9)
RequestProject/Spine/GoodCover/GoodCoverAcyclic.lean    (WP10)
RequestProject/Spine/GoodCover/HardenedSpec.lean        (WP11–WP13)
```

### What was proved

- **WP1/WP2 audits.** The Task-5 DAG `f ↦ f_# ↦ f* ↦ Hmap` was inspected and reused unchanged; `Hmap_id` and `Hmap_comp` (contravariant order) confirmed theorem-level. The pinned library was audited: it has homotopies, homotopy equivalences, the singular simplicial set and the topological standard simplex, but **no prism operator, no chain homotopy for the singular complex, and no homotopy invariance**. Mathlib's `ContinuousMap.Homotopy`/`HomotopyEquiv` are consumed directly; no second notion of homotopy was introduced.
- **WP3 the prism.** A singular simplex in the Task-5 model *is* a map `|Δᵏ| ⟶ X`, so the classical affine prism `p_i : Δⁿ⁺¹ → Δⁿ × I` is encoded by a pair of simplex-category morphisms (a codegeneracy for the `Δⁿ` direction, a "cut" map for the interval direction). `prismPiece`, `prism`, and the cochain operator `prismK : Cⁿ⁺¹(Y;ℤ₂) →ₗ Cⁿ(X;ℤ₂)` are built from that. The identity `δ∘K + K∘δ = g* + f*` (`prismK_identity`, and its degree-0 form) is **proved**, not postulated — the off-diagonal faces of the prism cancel against the prism simplices of the faces, and the degenerate faces telescope to the two endpoints.
- **WP4 sign audit.** `CharTwo.signed_vs_unsigned` proves the exact translation between `∂P + P∂ = g_# − f_#` and `∂P + P∂ = g_# + f_#`; `alternating_coboundary_eq_coboundary` proves the Task-5 unsigned coboundary is the mod-2 reduction of the alternating one. The collapse is a real edge of the proof DAG (used in the main theorem).
- **WP5/WP6.** The cochain-homotopy relation in the exact Task-5 coboundary convention, and `pullback_add_pullback_mem_coboundaries`, which explicitly consumes `δα = 0`.
- **WP7 main theorem.** `Mod2Cohomology.Hmap_eq_of_homotopic : f ≃ g → Hmap f n = Hmap g n`, **for all degrees `n`** (the degree-2 fallback was not needed). The result lives in the genuine Task-5 quotient, via `mk_surjective` and `mk_eq_mk_iff`.
- **WP8.** `homotopyEquivCohomology` — forward map is `Hmap` of the equivalence, inverse is `Hmap` of its homotopy inverse, both by `rfl`; the inverse laws come from `Hmap_comp`, `Hmap_id` and the new invariance. No arbitrary transport.
- **WP9.** Contractible ⇒ `Hⁿ⁺¹ = 0` in **all** positive degrees, with `H¹ = 0` and `H² = 0` recorded separately; it reuses the existing point computation rather than reopening it.
- **WP10.** `GoodCoverZ2.isAcyclicCover_of_isGoodCover` — good cover ⇒ acyclic cover, now unconditional.

### Interface hardening (no proofs of the remaining edges, as instructed)

- **WP11.** The old homotopy-invariance interface is retained for provenance, relabelled `SUPERSEDED_BY_NATIVE_THEOREM` in its own docstring, and *inhabited* by `nativeHomotopyInvariance`.
- **WP12.** `NervePresentation` + `NerveRealizationSpec` force the realization to be `SSet.toTop` of a simplicial set whose simplices and faces are those of the existing combinatorial nerve; the four layers are separate fields, and a map to the old interface shows the new one is strictly stronger.
- **WP13.** `SimplicialSingularCochainMap` (degreewise map + commutation with both coboundaries) descends here to cocycles, coboundaries and cohomology; a comparison may only assert bijectivity of *that induced map*. An arbitrary linear isomorphism no longer qualifies.
- **WP14.** `HardenedComparison` has exactly two fields — the two remaining blocked edges — and no homotopy-invariance field, in every degree; the Spin-lift class is transported along it (`spinLiftSingularClassHardened`, `frameSingularClassHardened_eq_zero_iff_spinStructure`).

### Answers to the final questions

1. **Yes** — homotopy invariance is theorem-level, in every degree, from a genuine prism/chain-homotopy construction.
2. **Yes** — the homotopy-equivalence isomorphism is `Hmap` in both directions; nothing is supplied as comparison data.
3. **Yes** — contractibility gives vanishing in all positive degrees, and good covers are acyclic covers.
4. **Remaining blocked edges:** the nerve theorem (now specified by `NerveRealizationSpec`, `BLOCKED_PENDING_GEOMETRIC_REALIZATION`) and simplicial-to-singular comparison (`SimplicialSingularComparisonSpec`, `BLOCKED`); carried over from Task 7, existence of good covers and of a common good refinement remain open, so cover-independence stays conditional. Homotopy invariance is no longer on the list — exactly one major blocked edge was removed.
5. **Yes** — both provisional interfaces are now specified so that neither an arbitrary space nor an arbitrary linear isomorphism can satisfy them, and both old declarations carry explicit "must never be documented as a proved comparison theorem" labels.

### Documentation

`TASK08_AUDIT.md` (WP-by-WP results, the dependency DAG, the failbuild ledger with the exact command, diagnostic, cause, repair and statement-change classification for all five failed builds, full build evidence and the axiom audit), `TASK08_EXTERNAL_SOURCES.md` (no external formalization was inspected; the Task-5 ledger is preserved verbatim), and an appended Task-8 section in `DEPENDENCY_DAG.md`. The firewall now also audits eight Task-8 proof closures. Import isolation re-verified: Experiment1 and Experiment2 direct and transitive imports are 0, external-project imports 0.


# Summary of changes for run 51dd3283-d3ad-4a36-af37-99ba0fb2ce99
**Task 7 delivered as a new Lean layer `RequestProject/Spine/GoodCover/**` (9 modules + endpoint), plus `TASK07_AUDIT.md`.** `lake build` is green (8184 jobs), the mechanical firewall passes, and the new layer contains no `sorry`, `admit`, project-local axiom, `unsafe`, `partial` or `implemented_by`; all 36 principal new declarations report only `[propext, Classical.choice, Quot.sound]`.

**Proved unconditionally (cover side).**
- *WP3* — `GoodCoverZ2.IsGoodCover` (every nonempty finite intersection of cover members contractible, in exactly the nerve form the Čech layer uses), the implication chain contractible ⇒ path connected ⇒ connected ⇒ preconnected, and a control that the chain is strict. Endpoint: **a good cover makes the Task-6 constancy conditions automatic** (`goodCover_constOn`), so the whole Task-6 Spin package becomes hypothesis-free on a good cover — including `frameCechClassGood_eq_zero_iff_spinStructure` (Čech class = 0 ⟺ a Spin structure exists).
- *WP3(b)* — acyclic covers are defined **separately**, with respect to the native singular theory, and are not identified with good covers.
- *WP5* — the nerve is exhibited as an abstract presimplicial set whose ℤ₂ simplicial cochain complex is built independently (own `δ²=0`), and the Task-6 Čech complex **is** that complex: `Ȟⁿ(𝓤;ℤ₂) = Hⁿ_simp(N(𝓤);ℤ₂)`, the comparison being the identity.
- *WP2/WP4* — the Task-4 manifold class is strongly locally contractible, every point has arbitrarily fine **contractible open** neighbourhoods, and every open cover is refined by one with contractible members (`exists_contractible_open_nhds`, `exists_contractible_refinement`, `contractibleCover`). Good-cover *existence* is **not** proved: it needs Riemannian input absent from the pinned library, so the theory is developed parametrically under `IsGoodCover` (Outcome C).
- *WP9 (partial)* — the singular mod-2 cohomology of a one-point space vanishes above degree zero, computed directly in the native theory.
- *WP13* — on a good cover the map into the older Task-3 obstruction carrier is injective; the surplus Task-3 representatives are artefacts of the broader carrier, so it can be deprecated for certification while being retained for provenance.

**Proved conditionally, on named missing theorems (not axioms, not typeclasses, not consumed by production).** The nerve theorem, the simplicial-to-singular comparison and homotopy invariance are recorded as explicit hypothesis structures; from them the module derives `Φ_𝓤 : Ȟ²(𝓤;ℤ₂) ≅ H²_sing(M;ℤ₂)`, `spinLiftSingularClass` with independence of the local Spin lifts, **`[z]_sing = 0 ⟺ [z]_Ȟ² = 0 ⟺ coherent Spin lifts exist`**, the frame form `[z]_sing = 0 ⟺ a Spin structure exists`, refinement invariance (WP12), and `good ⇒ acyclic` — the last showing homotopy invariance is the sole missing input there. No artificial Čech-to-singular cochain map was invented (WP7). `w₂(TM)` is **not** defined; only the interface an independent construction must satisfy, and the statement to be proved about it (WP14).

**Answers.** (1) Suitable good covers do **not** exist natively for the Task-4 manifold class and are not derivable in the pinned library; what is derivable and proved is the weaker contractible-member cover, so good covers enter as an explicit hypothesis. (2) The Spin-lift class is canonically represented in `Ȟ²(𝓤;ℤ₂) = H²_simp(N(𝓤);ℤ₂)`; its passage to `H²_sing(M;ℤ₂)` exists only relative to the three named hypotheses, and the resulting isomorphism depends on a chosen nerve equivalence — it is not canonical. (3) Cover independence is proved only for refinements of good covers, conditionally; full independence needs common good refinements, stated exactly and left unproved. (4) The minimal independent next construction is **homotopy invariance of the native singular mod-2 theory** — the unique first node shared by the comparison route and by both routes to an independent `w₂`.

`TASK07_AUDIT.md` records the infrastructure audit with classifications, the manifold-hypothesis audit, the blocked-edge table (source, target, theorem needed, available primitives, missing infrastructure, scope), a 16-entry failbuild ledger plus one self-review correction, build evidence, isolation figures (Experiment1/Experiment2 direct and transitive imports = 0, external-project imports = 0) and the axiom audit; `ARCHITECTURE.md`, `DEPENDENCY_DAG.md` and the external-source ledger were extended accordingly. Throughout, `[z]` and `[z]_sing` are treated purely as topological Spin-lift obstructions.

# Summary of changes for run 8259211f-8a55-4172-ba5e-d21a56b2b5b6
**Task 6 delivered: a native fixed-cover Čech ℤ₂-cohomology theory inside the Spine, with the Spin-lift obstruction realised as a genuine class `[z] ∈ Ȟ²(𝓤;ℤ₂)` — plus the requested Task-5 documentation corrections.** `lake build` is green (8174 jobs), with no `sorry`, no `admit`, no project-local axiom, no `unsafe`/`partial`/`implemented_by`; every principal new declaration reports only `[propext, Classical.choice, Quot.sound]`.

**New Lean layer — `RequestProject/Spine/Cech/**` (12 modules + endpoint).**
- *Spin-independent half (WP2–WP4, WP11):* the nerve of a cover (tuples with nonempty overlap) and `Čⁿ(𝓤;ℤ₂) = Nerve → ZMod 2` as a genuine function type with derived module structure; the coboundary, with a **theorem** that it equals the conventional alternating formula (signs collapse since −1 = 1 in ℤ₂), and the **theorem `δ² = 0`** proved from a `Fin.succAbove` simplicial identity; `Žⁿ`, `B̌ⁿ`, the theorem `B̌ⁿ ⊆ Žⁿ`, and the actual quotient `Ȟⁿ(𝓤;ℤ₂)` (so `Ȟ¹`, `Ȟ²`); the refinement pullback `r*` with both functor laws. These four modules import Mathlib only, and the build-time firewall now *enforces* that they reach no Spin, lift, geometry or cohomology module.
- *Bridge (WP5–WP10):* the kernel dictionary `ker ρ ≅ ℤ₂` as data (+1↦0, −1↦1, multiplication↦addition, packaged as a `MulEquiv`), instantiated by reusing the existing sign map; the existing triple-overlap defect translated into a Čech 2-cochain; **`δ_Č z = 0`** derived from the existing multiplicative quadruple-overlap identity; **`z′ = z + δ_Č a`** for change of lifts, hence a lift-independent class; and **`[z] = 0 ⟺ coherent Spin transition data exist`** (frame form: ⟺ a Spin structure exists). The one extra hypothesis — that the defect/comparison cochain is constant on the relevant overlaps — is explicit data, constructed from local constancy when the overlaps are preconnected; no good cover, acyclicity or direct limit is assumed anywhere.
- *Comparison (WP9), the central certification step:* a canonical map `Ȟ²(𝓤;ℤ₂) → ObstructionClass`, carrying the new Spin class to the old obstruction class, **injective** on covers with preconnected double overlaps, and **provably not surjective** in general. Honest classification: **EMBEDS_CANONICALLY** — not definitionally equal, not isomorphic; the old carrier is strictly larger (arbitrary group-valued cochains, no cocycle condition). Both vanishing criteria are proved equivalent, and refinement naturality `r*[z_𝓤] = [z_𝓥]` plus independence of the refinement index map are proved.
- *WP12:* the remaining map `Φ_𝓤 : Ȟ²(𝓤;ℤ₂) → H²_sing(X;ℤ₂)` is specified exactly as a hypothesis structure (with conditional transport and refinement invariance), never constructed and never assumed as an axiom; the three possible routes are audited, and the minimal next theorem is named: a degree-two cochain-level comparison sending Čech cocycles/coboundaries to singular ones.

**Maintenance.** `TASK05_AUDIT.md` now records the actual final module count (134 Spine modules in the archive, 132 in the audit environment; after Task 6: 146 and 144, with the two-module gap explained). The gq2 provenance entry is corrected in `TASK05_EXTERNAL_SOURCES.md`: repository `https://github.com/roed314/gq2`, inspected commit `a2b1481f…`, documenting `GQ2.StiefelWhitney` as Hasse–Witt invariants of quadratic forms in Galois cohomology — recorded permanently as **NEGATIVE_CONTROL, not used**. No mathematical Task-5 result was touched, no Task-3 declaration was modified or removed, and no external project is a dependency (external-project imports = 0, Experiment1/Experiment2 direct and transitive imports = 0).

**Documentation.** `TASK06_AUDIT.md` contains the WP1 audit and dependency DAG with every relevant Task-3 declaration classified, the WP2–WP12 record, the comparison analysis with the exact mismatch, a 15-entry failbuild ledger (every failure with cause, repair and `STATEMENT_UNCHANGED` classification), build evidence for all required and new targets, and the axiom audit. `ARCHITECTURE.md` and `DEPENDENCY_DAG.md` record the new layer.

**Answers.** (1) Yes — the obstruction is now a genuine class in a Spin-independent Čech theory, subject to the explicitly stated overlap-constancy hypothesis that constant ℤ₂ coefficients require. (2) The earlier `ObstructionClass` is neither definitionally equal nor canonically equivalent to it: the Čech group embeds canonically (injectively, under preconnected double overlaps) and the embedding is not surjective. (3) The minimal next theorem is a ℤ₂-linear degree-two cochain map `Č²(𝓤;ℤ₂) → C²_sing(X;ℤ₂)` respecting cocycles and coboundaries; everything downstream of it is already proved conditionally. Throughout, `[z]` is treated purely as a topological Spin-lift obstruction — not curvature, torsion, holonomy or any dynamical quantity — and no identification with `w₂(TM)` is claimed.

# Summary of changes for run 5147f165-45b2-42b4-8dfe-42fcff4e18a0
**Task 5 delivered: a genuine native mod-2 singular cohomology layer inside the Spine, plus the full external-reference audit.** `lake build` is green (8162 jobs), with no `sorry`, no `admit`, no project-local axiom, no `unsafe`/`partial`/`implemented_by`; every principal new declaration reports only `[propext, Classical.choice, Quot.sound]`.

**New Lean layer — `RequestProject/Spine/Cohomology/**` (8 modules + endpoint):**
- **WP2** `Cochain.lean`: singular `n`-simplices as the values of Mathlib's singular simplicial set `TopCat.toSSet`, a named reindexing operation with functoriality, the face operators, and `Cⁿ(X;ℤ₂) = Sing_n X → ZMod 2` — a genuine function type with the derived module structure (no opaque carrier, no typeclass-supplied cohomology).
- **WP3** `Coboundary.lean`: `δ` as a `ZMod 2`-linear map, with the explicit reason the signs disappear (`-1 = 1`), and the **theorem `δ² = 0`**, proved from the Mathlib cosimplicial identity `SimplexCategory.δ_comp_δ` — not postulated.
- **WP4** `Cohomology.lean`: `Zⁿ = ker δ`, `Bⁿ = im δ` (with `B⁰ = 0`), the **theorem `Bⁿ ⊆ Zⁿ`**, and `Hⁿ(X;ℤ₂) = Zⁿ/Bⁿ` as an actual quotient module, degree-generic (so in particular `n = 0,…,4`).
- **WP5** `Functorial.lean`: pullback `f*` on cochains, `f*∘δ = δ∘f*` from naturality, the induced `Hⁿ(Y)→Hⁿ(X)`, and both functor laws.
- **WP6** `AlexanderWhitney.lean` / `Cup.lean` / `CupDescent.lean`: the four front/back inclusions and six coface identities, the cup product, a **degree-generic Leibniz rule**, and the descended bilinear products `H¹×H¹→H²`, `H²×H²→H⁴` with their cup squares.
- **WP8** `CechBridgeSpec.lean`: the *specification* of the missing Čech-to-singular comparison as an explicit hypothesis (`ComparisonDatum`), with a conditional transport theorem. The fixed-cover obstruction quotient is **not** identified with `H²_sing` anywhere.

**Audits and isolation.** The mechanical firewall was extended with an external-isolation check run over all 132 Spine modules: Experiment1/Experiment2 direct and transitive imports = 0, external-project imports = 0; the seven pure-cohomology modules provably reach no `E1`/`E2`/`Geometry`/`Controls` module (Mathlib only), with a positive control that the bridge spec *does* reach the Čech obstruction. No git dependency, copied file, vendored source or external `.olean` exists.

**Documentation.** `TASK05_AUDIT.md` (Mathlib availability matrix, WP2–WP6 results, the native-vs-external comparison table, the exact Čech/singular type-gap analysis with its dependency specification, the audit of the three future `w₂` routes, the full certification DAG with every missing edge marked, the recommended next node, failbuild ledger, build evidence, axiom audit) and `TASK05_EXTERNAL_SOURCES.md` (append-only ledger: owner, repository, files, commit hash, access date, licence status, concepts and ideas consulted, influence, and citation text for each source). `ARCHITECTURE.md`, `DEPENDENCY_DAG.md` and `PROVENANCE_LEDGER.md` were extended accordingly.

**Answers to the final questions.** (1) Yes — `H²(X;ℤ₂)` is now a genuine native carrier (a real quotient of the kernel of an explicit linear map), reconstructed without importing or copying any external formalization; the construction is the classical one and converges with prior external work, which is credited, and no novelty is claimed for the reimplementation. (2) The shortest remaining chain has two independent halves that must *both* be completed: a carrier comparison (Čech `ℤ₂` complex → identification with `Ȟ²` of the cover → refinement colimit → a good-cover/paracompact Čech-to-singular comparison) and an independent construction of `w₂` (either fundamental class → Poincaré duality → Wu class → Wu formula, or Thom space → Steenrod squares → Stiefel–Whitney classes). Defining `w₂` to be the obstruction is rejected as circular. The recommended next node — the only remaining edge whose inputs already exist — is a native Čech `ℤ₂` cochain complex plus the identification of the Task-3/Task-4 class with `Ȟ²` of the cover. (3) Prior exploration exists in NetRxn/SK_EFT_Hawking (singular mod-2 cohomology, cup product, `v₂`/Wu route; also a negative control for typeclass-supplied Stiefel–Whitney data), trigunino/janus-formal and TataSatyaPratheek/mathematical-physics-lean (bundle designs; the latter also a trust-boundary negative control, as it rests on 362 project-local axioms), plus an inaccessible name-collision control; all are credited by author, repository, file and commit in the ledger even though no code was copied.

# Summary of changes for run 26b1aa07-20f4-4d3a-8dda-5b67c059e5dc
Task 4 is delivered as a new, fully machine-checked native layer `RequestProject/Spine/Geometry/**` (plus one downstream control). `lake build` is green (8153 jobs) with no `sorry`, no `admit`, no project-local axiom, no `unsafe`/`partial`/`implemented_by`; every principal declaration reports only `[propext, Classical.choice, Quot.sound]`; the mechanical legacy firewall was extended to the new modules and reports Experiment1/Experiment2 direct and transitive imports = 0 for all of Spine (123 modules audited).

**Answer to the first final question — vanishing equivalence only, and the comparison partner does not exist formally.** The instantiation itself is complete and theorem-level, but the pinned library has no Stiefel–Whitney classes, no principal bundles, no Spin structures, no manifold orientation, no pseudo-Riemannian/Lorentzian metrics and no `H²(M;ℤ₂)`, so `[c]_frame = w₂(TM)` cannot even be *stated*. `w₂` was deliberately **not** defined as "the obstruction being studied" (that would be circular). Classification: `VANISHING_EQUIVALENCE_ONLY` plus a canonical ℤ₂-Čech presentation of the carrier; literal equality is `BLOCKED` at library level, not conceptually.

**Answer to the second final question — soldering is the next unresolved bridge.** Before Spin data can determine geometry rather than lift frame structure one needs (i) a total-space principal `SO⁺(1,3)`-frame bundle with its `Spin⁺(1,3)`-lift (missing principal-bundle infrastructure), then (ii) a **solder form** identifying the associated bundle with `TM`, and only then (iii) a connection, curvature and torsion. (ii) is identified, not solved.

What was built and proved:
- **WP1** availability matrix of manifold/bundle/characteristic-class infrastructure (what Mathlib supplies, what was built natively, what is absent and large).
- **WP2** orientation and time orientation as *separate* data with separate compatibility conditions, and the derived reduction criterion: a carrier automorphism preserving the intrinsic form, sending the unit into the future, with positive determinant lies in the native proper orthochronous group `GLor`. Three negative controls show orientation and time orientation are independent and neither hypothesis is redundant. No identification with an externally presented `SO⁺(1,3)` is assumed: the native `GLor` *is* the target.
- **WP3** `LorentzFrameData` (cover, fibrewise metric, orientation form, timelike time-orientation field, local frames constrained only on their patches) and the derived `GLor`-valued frame transition cocycle with `g_ii = 1`, `g_ji = g_ij⁻¹`, `g_ij g_jk = g_ik` and continuity on overlaps.
- **WP4** the Task-3 obstruction instantiated on that cocycle: `{±1}`-valued defect, class independent of the chosen local Spin representatives.
- **WP5** the Čech notion of a Spin structure on the frame data, its equivalence with coherent Spin-valued transition data, and the endpoint `[c]_frame = 0 ↔ a Spin structure exists`.
- **WP7** the canonical `{±1} ≅ ℤ/2` identification, the additive ℤ₂ Čech cochain with the standard 2-cocycle and coboundary laws, and the additive vanishing criterion.
- **WP8** the fixed-cover torsor statement (difference/twist by continuous ±1-valued Čech 1-cocycles are mutually inverse); passage to isomorphism classes and `H¹(M;ℤ₂)` is deferred.
- **WP9** signature/component audit: `(1,3)` signature, dimension 4 (carrier and every tangent space), properness, orthochronicity, kernel of order 2.
- **Tangent instance** the fibres are the genuine Mathlib tangent spaces of a smooth manifold modelled on the four-dimensional intrinsic carrier.
- **Controls** a non-vacuity witness (frame data that really exists and carries a Spin structure) and a formal refutation of uniqueness: two distinct Spin structures on the same frame data.

Explicit boundaries are stated in every module docstring: vanishing of the obstruction means only topological admissibility of a Spin lift — not bundle triviality, not uniqueness of the Spin structure, and nothing about curvature, torsion, connections, gravity or any synchronization notion; the uploaded hypothesis paper was used as motivation only, never as a premise.

Documentation: `TASK04_AUDIT.md` (final answers, availability matrix, dependency DAG, orientation/time-orientation model, cocycle, obstruction instance, Spin-structure equivalence, `w₂` status with the minimal missing theory, canonicality/nonuniqueness tables, negative controls, anti-shortcut compliance, build evidence, axiom audit, append-only fail-build ledger with four entries — none of which changed a statement); `DEPENDENCY_DAG.md` and `ARCHITECTURE.md` were extended with the Task-4 layer, and `Spine/Core.lean` now aggregates it. All work is committed and pushed.

# Summary of changes for run be199760-1406-467e-8ff4-af4ada641e26
Task 3 is complete: a new, fully machine-checked native layer `RequestProject/Spine/E2/Cech/**` builds green (`lake build`, 8146 jobs) with no `sorry`, no `admit`, no project-local axiom, no `unsafe`/`partial`/`implemented_by`, and the firewall audit passing.

**Answer to the final scientific question — yes.** The native intrinsic Spin projection `SpinCore.internalSpinProjection` does generate a well-defined ℤ₂-valued Čech lifting obstruction for the visible transition data, invariant under change of local Spin representatives and natural under cover refinement, whose vanishing is exactly equivalent to the existence of coherent Spin-valued transition data — on a fixed cover, and for transition data that admits local lifts on the double overlaps (that liftability is an explicit hypothesis, never assumed silently). **Is it w₂(TM)? Not proved and not claimed — it remains open**: there is no manifold, tangent bundle, metric, orientation or frame bundle anywhere in the project, so the identification cannot even be stated. At most: the object is an abstract ℤ₂-valued Spin-lift obstruction of the correct formal type for a later comparison.

What was built and proved (all statements are theorems in the project):
- **WP2** `CechCover`, `VisibleCocycle` over an abstract topological base; only the triple-overlap cocycle law is assumed, `g_ii = 1` and `g_ji = g_ij⁻¹` are derived (`visible_transition_laws`).
- **WP3** the exact sufficient criterion for a lift on a whole overlap, unconditional pointwise-local liftability (`exists_lift_nhdsWithin`), the `SpinLiftFamily` data and the `IsLiftAdmissible` hypothesis with proved sufficient criteria (section domains; constant transitions; fine refinements).
- **WP4** the defect `c_ijk = g̃_ij g̃_jk g̃_ik⁻¹`, with `ρ(c)=1`, kernel-valuedness, continuity and local constancy (from discreteness of the kernel) all derived, not postulated.
- **WP5** the exact multiplicative Čech 2-cocycle law `c_jkl c_ikl⁻¹ c_ijl c_ijk⁻¹ = 1`, read off the definitions rather than hard-coded, plus an explicit record of exactly where centrality of the kernel is required (and where mere kernel-valuedness suffices).
- **WP6** the change-of-representative/coboundary law `c' = (δε) c` with `ε` continuous and kernel-valued.
- **WP7** a lightweight native quotient (built on `Quot` only, no cohomology library) and the first invariant output: `obstruction_lift_independent`.
- **WP8** both directions of `[c] = 1 ↔ coherent Spin-valued transition data exists`, plus the explicit kernel-adjustment form.
- **WP9** pullback of covers, transition data, lifts, defects and classes; refinement naturality `[c_𝓥] = r*[c_𝓤]`; functoriality of the restriction map; and independence of the restricted class from the chosen refinement index map.
- **WP10** instantiation with the native Spin projection: `c_ijk ∈ {1, −1}` and the packaged endpoint `SpinCore.spinLiftObstruction`. Everything below the instantiation module is free of any concrete projection — mechanically checked.

Also added: a downstream control exhibiting a legitimate lift family whose defect is `−1` while the class of the same data vanishes, so the choice-dependent and choice-invariant layers are demonstrably distinct and none of the theorems are about a trivial object.

Negative results are reported rather than repaired by strengthening assumptions: (a) existence of a lift-admissible refinement for an arbitrary base is **not** proved (it needs a shrinking/paracompactness hypothesis or a good cover, both classified as additional assumptions), and the defect algebra is unaffected by this gap; (b) no cover-independent direct-limit Čech class is constructed, as the task permits.

Constraints honoured: zero direct and zero transitive imports of the historical experiment trees for all active Spine modules (firewall + audit script), no Task-23 infrastructure, no identification of kernel signs with matter/antimatter, no reading of vanishing as flatness, and the mandatory separation "topological ℤ₂ obstruction ≠ dynamical synchronization defect" stated in both the sources and the audit. The uploaded hypothesis paper was used only as background context, never as a formal assumption.

Documentation: `TASK03_AUDIT.md` (dependency DAG, WP1 classification of every inherited/missing ingredient, canonicality table, the exact-obstruction reports, build evidence, axiom audit and an append-only fail-build ledger), with `ARCHITECTURE.md` and `DEPENDENCY_DAG.md` extended for the new layer. All work is committed and pushed.

# Summary of changes for run 9e0f62da-8edc-461a-9ca2-9d776db03127
**Answer to the final scientific question: yes.** The intrinsically reconstructed continuous central Spin double cover `SpinCore.spinCover : SpinGroup →* GLor` **does** possess native continuous local sections around every point of `GLor`, and they suffice to instantiate the generic `InternalProjection` interface. Everything below is machine-checked: the whole project builds green with no `sorry`, `admit`, project-local `axiom`, `unsafe`, `partial`, `implemented_by` or `native_decide`.

**WP1 (verified from source).** Of the six fields of `InternalProjection`, five were already closed by the inherited results (`spinCover`, `continuous_spinCover`, `spinCover_surjective`, `spinCover_ker_central`, kernel discreteness); exactly one — continuous local sections — was open, as the previous package `TopologicalCentralCover` deliberately omits it.

**The mechanism (Route A: direct local construction; new module `RequestProject/Spine/E1/Topology/LocalSection.lean`).** The twisted Clifford action is inverted algebraically:
1. the contraction `fierz y = y + Σᵢ eᵢ y eᵢ` lands in the centre (checked in the intrinsic eight-monomial frame), with `fierz 1 = 4`;
2. the reconstruction `recon F = A(F·1) + Σᵢ A(F eᵢ)·eᵢ`, linear hence continuous in `F`, satisfies `recon (action of g) = g · fierz (reverse g)` — so it returns the spin element up to a *central* factor;
3. identifying the centre with ℂ (`cx : ℂ →ₐ[ℝ] Cl₃(ℝ)`, `i ↦ ω`), the central factor `w` is visible only through `spinDisc F = w²`, a genuine continuous function of `F` — this is exactly where the ℤ₂ sits;
4. on the open set where the discriminant avoids the branch cut (it equals `16` at the identity), dividing by the principal square root yields a continuous map whose value is `±u`, hence a genuine spin element and a section, normalised by `spinSection 1 = 1`;
5. left translation by any chosen lift transports it to a neighbourhood of every point: `SpinCore.spinCover_hasLocalSection`.

No covering-map or local-homeomorphism property is postulated, no `SL(2,ℂ)`/matrix model is used (mechanically enforced), and no manifold or Lie-group theorem is invoked.

**Local two-sheet structure (strongest justified form, proved).** Over the explicit identity neighbourhood the preimage is the union of two *disjoint open* sheets exchanged by `-1`; openness is derived (each sheet is the relative complement of the other, both relatively closed as equalisers into a Hausdorff group), the sheet containing `1` maps bijectively onto the neighbourhood, and the restriction is a homeomorphism (`sheetPlusHomeomorph`). The global statement "`spinCover` is a covering map" is not claimed.

**Canonicality, kept explicit.** Two lifts of the same element differ exactly by `-1` (`lift_ambiguity`), and changing the chosen lift multiplies the transported section by `-1` (`transportSection_negOneSpin`). Existence, uniqueness, uniqueness-modulo-kernel, dependence on the lift and local uniqueness after fixing a sheet are stated separately, never conflated. The projection structure itself carries no choice (all interface fields but `proj` are propositions); the branch/sheet and the lift are the only choices, and both only up to the central ℤ₂. No scalar synchronization-type parameter was introduced.

**WP7/WP8.** `RequestProject/Spine/E2/SpinProjection.lean` builds the native `SpinCore.internalSpinProjection : InternalProjection SpinGroup GLor` from the intrinsic results only (E1 never imports E2; no bridge to historical code), and `RequestProject/Spine/E2/SpinProjectionIntegration.lean` is an integration test in which the existing generic lift theory consumes it: local internal representatives, kernel ambiguity, Level-B candidates for any transition system valued in `GLor` (plus a concrete one-chart system so the statements are non-vacuous), and the kernel-valued, locally constant triple-overlap defect. No frame bundle, spin structure or geometric object was constructed.

**Verification.** `lake build` (8137 jobs) and each focused target succeed; `Spine.Audit.Firewall` prints `SPINE FIREWALL AUDIT: all checks passed`, now extended with checks that the new local-section module reaches no `Spine.E2`, `SL(2,ℂ)`, matrix-model, control or historical module, and that the new projection module reaches exactly the intrinsic core and the generic interface (with positive controls so the audit is sensitive). `scripts/legacy_audit.py` reports `RESULT: PASS` with Experiment1/Experiment2 direct and transitive imports = 0 for every endpoint, including the three new ones. All principal new declarations report exactly `propext, Classical.choice, Quot.sound`.

**Deliverables.** The Lean implementation (three new modules, plus endpoint re-exports in `Spine/E1/Topology/Core.lean`, `Spine/E1/Core.lean`, `Spine/Core.lean` and the firewall/audit-script updates), and `TASK02_AUDIT.md` containing the WP1 gap audit, the route classification, the new dependency DAG, the canonicality table, the explicit identity-neighbourhood argument, the exact two-sheet statement, the integration-test description, a 9-entry append-only failbuild ledger (no statement was ever weakened), build evidence, the axiom audit, and the final classification of every result (`DERIVED_NATIVE` / `INHERITED_CANONICAL` / `CHOICE_UP_TO_KERNEL` / `ADDITIONAL_CHOICE` / `NOT_REQUIRED`; nothing `BLOCKED`). `ARCHITECTURE.md`, `DEPENDENCY_DAG.md` and `README.md` were updated; `ARISTOTLE_SUMMARY.md` was read but not edited. All work is committed and pushed.

# Summary of changes for run 8c3f69b1-3b8b-44d8-9f23-40976e95b5bc
**Task 1 is complete and the answer is affirmative:** the intrinsic algebraic Spin/Lorentz double cover already in `Spine/E1` does canonically determine a continuous central two‑fold projection of Hausdorff topological groups with discrete kernel — with no input from the historical experiment trees and no use of the downstream SL(2,ℂ) comparison. The hypothesis paper was treated as context only; none of its claims is used as a premise.

**New Lean layer** (`RequestProject/Spine/E1/Topology/`, six modules, no `sorry`/`admit`/`axiom`/`unsafe`/`partial`/`implemented_by`):

- `FiniteDimTools.lean` — generic, Mathlib‑only: uniqueness of the Hausdorff vector‑space topology on a finite‑dimensional real space (it is the module topology), Hausdorffness of the module topology, and a basis‑evaluation criterion for continuity into a space of linear maps.
- `CliffordTopology.lean` (WP1/WP3) — the eight monomials of the existing intrinsic frame are packaged as a basis, giving `Module.Finite ℝ Cl₃(ℝ)` and `finrank = 8`; the topology installed is `moduleTopology ℝ Cl₃(ℝ)` — choice‑free, no basis/norm/matrix model in the definition — with `IsTopologicalRing`, `T2Space`, a homeomorphism `Cl₃(ℝ) ≃ₜ ℝ⁸`, and `Cl3_topology_unique` (any Hausdorff TVS topology equals it).
- `LorentzTopology.lean` (WP2) — the carrier's product topology is proved to *be* its module topology; `End(𝒮)` gets the module topology, hence is a Hausdorff topological ring (`EndLor_topology_unique`); its unit group carries Mathlib's canonical unit topology, transported to the linear automorphism group along Mathlib's canonical `MulEquiv`; `GLor` is then a Hausdorff topological group in the subgroup topology. Matrix and transported‑equivalence routes were compared and rejected as extra choices.
- `SpinTopology.lean` (WP3) — `SpinGroup` is a Hausdorff topological group purely by inheritance: Clifford algebra → topological ring → unit group → subgroup.
- `SpinCoverTopology.lean` (WP4/WP5) — continuity of `spinCover` is proved *from its own definition*: the choice‑defined action is eliminated via a linear retraction of the paravector embedding (`spinLor g x = paraRetract (g · A x · reverse g)`), giving continuity of the whole Clifford action `Cl₃(ℝ) → End(𝒮)` and hence of the cover (the identifications with `spinLorLin ↑u` and `spinLorLin (cconj ↑u)` hold by `rfl`). Kernel discreteness is derived from Hausdorffness of the source plus finiteness (re‑using the existing `Nat.card (ker) = 2`), not from cardinality alone. The results are bundled as `TopologicalCentralCover` / `topologicalSpinProjection` — deliberately the E2 interface *minus* local sections, which are not claimed.
- `Core.lean` — endpoint `spin_double_cover_topological`, re‑exported from `Spine/E1/Core.lean`.

**Deliverable document** `TASK01_AUDIT.md`: carrier audit, the full dependency DAG of the new layer, a 20‑row canonicality/provenance table (only two entries are free choices, both proof‑internal and absent from every statement), the 8‑entry failbuild ledger, exact build commands and outputs, the axiom audit, the classification of each property (`DERIVED_NATIVE` / `INHERITED_CANONICAL` / `TRANSPORTED_CANONICAL` / `ADDITIONAL_CHOICE` / `NOT_REQUIRED`; nothing `BLOCKED`), and the final answer. `ARCHITECTURE.md`, `DEPENDENCY_DAG.md`, `README.md` and the `Spine/Core` docstring were updated.

**Verification**: `lake build RequestProject.Spine.E1.Core` (8054 jobs), `…E2.Core` (8091), `…Spine.Core` (8123), the new `…E1.Topology.Core` (8051) and the full `lake build` (8134) all complete successfully; `RequestProject.Spine.Audit.Firewall` prints `SPINE FIREWALL AUDIT: all checks passed` — extended so the build now fails if any of the six new modules ever reaches `Experiment1/**`, `Experiment2/**`, `Spine.Controls/**`, `Spine.E2/**`, `SL2Comparison`, `MatrixModel`, `PauliRepresentation` or `WeylSpinorModule`. `scripts/legacy_audit.py` reports `RESULT: PASS` with `Experiment1 = 0`, `Experiment2 = 0` direct and transitive for every endpoint, including the new one. All 31 audited declarations report exactly `propext, Classical.choice, Quot.sound`.

The question of continuous local sections is left untouched, as instructed. All work is committed and pushed.

# Summary of changes for run cf99807f-004d-4eb6-8004-06e88c5bfb1b
## Refactoring Task 03 — complete: the new Spine is fully legacy-independent

### Final verification (all re-run and confirmed in this session)

- **In-place build**: `lake build` → `Build completed successfully (8128 jobs)`, with `SPINE FIREWALL AUDIT: all checks passed`.
- **Isolated clean build**: a separate copy of the project with `RequestProject/Experiment1/**` and `RequestProject/Experiment2/**` **deleted** and an empty project build directory → `Build completed successfully (8128 jobs)`, `EXIT=0`, firewall audit passed. This mechanically demonstrates the success criterion: the historical experiments can be removed from the source tree without breaking any theorem in the new Spine.
- **Source-level audit**: `python3 scripts/legacy_audit.py` → `RESULT: PASS`. Per-endpoint direct and transitive legacy imports are `Experiment1 = 0`, `Experiment2 = 0` for every endpoint, including `Spine.Core`, `Spine.E1.Core`, `Spine.E1.SL2Comparison`, `Spine.E2.Core`, `Spine.E2.Topology.Task27`, `Descent.Task29`, `Global.Task30`, `Defect.Task31`, `AtlasChange.Task32`, `Spine.Audit.Firewall`, and all controls.
- **Proof discipline**: no `sorry`/`admit`/custom axioms anywhere under `Spine/**` (100 files searched). The 387 `#print axioms` reports in the isolated build resolve to only `propext`, `Classical.choice`, `Quot.sound` — no other axiom set occurs.

### What the refactoring delivered

- The concrete legacy projection path (`Spine/E2/Model/CertifiedProjection` → `Task23` → ~180 historical modules) and `ModelBridge.modelInternalProjection` were **removed**, not wrapped. `Spine/E2/Model/` no longer exists; its genuinely generic parts were moved into `Spine/E2/Lift/TransitionSystem.lean` and `Spine/E2/Lift/LiftLevels.lean`.
- The whole E2 chain is now parameterized by `P : InternalProjection L G` (liftability, descent, defect, global, atlas-change, family neutrality, the family classification). Negative results carry an explicit hypothesis instead of a hard-coded model. A new generic negative test object, the tautological transition system `tautSystem G`, replaces the historical model-specific example.
- Retained mathematical controls were **rebuilt natively** under `Spine/Controls/**`, using Mathlib alone: a genuine nontrivial central double cover (circle squaring, with continuous local sections and a two-element discrete central kernel), the counterexample that it has no global continuous section, and the regression test distinguishing local from global liftability. Production never imports controls; the firewall enforces the one-way direction.
- The three legacy-bridge modules under `RequestProject/Comparison/**` were deleted (recorded as SUPERSEDED); the E1 chain `LorentzCarrier → LorentzQuadratic → LorentzClifford → SpinGroup → spinCover → SpinKernel` with `SL2Comparison` downstream was verified mechanically to be legacy-free.
- `Spine/Audit/Firewall.lean` is a compile-time audit: it fails the build on any direct or transitive legacy import, on a production module importing a control, on a layer-order violation, or on a forbidden constant appearing in a principal endpoint's proof closure — with positive controls proving the audit is sensitive rather than vacuous.
- `lakefile.toml` separates the default new-project library from a non-default `LegacyProvenance` library, so the historical trees remain recoverable as provenance without participating in the production build.

### Documentation in the project

`LEGACY_DECOUPLING_AUDIT.md` (provenance table with migration classifications, direct/transitive import audit, axiom audit, before/after module and LOC statistics, isolated-build record, 9-entry fail/repair ledger), plus `ARCHITECTURE.md`, `DEPENDENCY_DAG.md`, `PROVENANCE_LEDGER.md`, `REFACTOR_AUDIT.md`, `README.md`.

Headline statistics: Spine = 100 modules, 20 686 LOC. `Spine.E2.Core` project-local closure went 247 (180 legacy) → 65 (0); Task29/30/31/32 216/228/238/246 → 34/46/56/64; `Spine.Audit.Firewall` 320 (219 legacy) → 98 (0); E1 endpoints unchanged at 22 (0 legacy).

As instructed, the E1→E2 topological bridge was **not** attempted: no topological group structures on the Spin group, continuity of `spinCover`, kernel discreteness, local sections, w₁/w₂, frame bundles or Spin connections were added. The point of this task was to guarantee that those will be built on an independent codebase.

All work is committed and pushed.

# Summary of changes for run a4ecf22e-f76a-4a88-938a-c6bd35f34a30
The intrinsic Spin double-cover core is now a self-contained endpoint of the Experiment-1 spine, with SL(2,ℂ) strictly downstream. The whole tree builds green (`lake build`: 8349 jobs, 0 errors) and the mechanical firewall reports `SPINE FIREWALL AUDIT: all checks passed`.

**A/B/C — intrinsic core (new modules under `RequestProject/Spine/E1/`).** The layering is now `Carrier → Clifford → LorentzClifford → CliffordMonomial → SpinElement → SpinGroup → Paravector/SpinDeterminant/DoubleCover → SpinCover → SpinKernel`, with `SL2Comparison` only after it.
- predicate vs. bundled group are separated: `SpinCore.IsSpinElem` stays in `SpinElement.lean` (the old `SpinGroup.lean`, renamed via `git mv`), and the bundled `SpinCore.SpinGroup : Subgroup Cl3ˣ` is new in `SpinGroup.lean`, together with `spinUnit`, `mkSpin`, the central element `negOneSpin`, `negOneSpin_sq`, `negOneSpin_ne_one`;
- `SpinCore.spinCover : SpinGroup →* GLor` and `spinCover_surjective` are in `SpinCover.lean`, built from the Clifford conjugation action already established on the Lorentz carrier;
- the exact kernel is in `SpinKernel.lean`, in four strengths: `mem_ker_spinCover_iff` (`u ∈ ker ↔ u = 1 ∨ u = -1`), `spinCover_ker_eq_zpowers` (`ker = ⟪-1⟫`), `spinCover_ker_card` (`Nat.card ker = 2`), `spinCover_ker_central`, plus the bundled `spin_double_cover`.

**No SL(2,ℂ) is needed.** The one place where the previous double-cover proof leaned on a matrix model was the centre theorem `SpinCore.center_Cl3`, previously derived from the centre of M₂(ℂ). It is reconstructed intrinsically in the new `CliffordMonomial.lean`: the eight-monomial frame (moved unchanged), its linear independence decided in the real 4×4 frame the project already used, the sign of conjugation by a generator, hence the centre — with no complex algebra. `one_omega_indep` was likewise reproved. The intrinsic chain up to `SpinKernel` therefore imports neither the matrix model nor the Pauli representation nor the comparison layer.

**D — comparison layer.** `SL2Comparison.lean` now imports the intrinsic core and owns only the comparison: `isSpinElem_iff_det`, `spinEquivSL2 : SpinGroup ≃* SL(2,ℂ)`, the compatibility `spinEquivSL2_negOne` (kernel element `-1` ↦ `-I₂`), and the bundled `sl2_comparison`. It owns no foundational declaration of the double cover.

**Firewall.** A new level-1b audit re-computes the import closure of the eleven intrinsic modules and rejects `Experiment1/**`, `Experiment2/**`, `Comparison/**`, `Control/**`, `Spine.E2.**`, `SL2Comparison`, `MatrixModel`, `PauliRepresentation`, `WeylSpinorModule`; two positive controls pin the comparison edge in the one allowed direction. Violations fail the build.

**Docs (E, F and the audits).** `SPIN_CORE_EXTRACTION_AUDIT.md` records: the new layering; a provenance table for every moved/reconstructed declaration (old Spine name, historical origin, new name, whether moved unchanged or reconstructed, imports removed); the firewall spec; before/after project-local dependency statistics (`Spine.E1.Core` 19→22, `Spine.E1.SL2Comparison` 18→22 — both increases are file splits — and the new intrinsic endpoint `Spine.E1.SpinKernel` at 19 with none of the matrix/comparison modules; `Spine.E2.Core` unchanged at 247/180 legacy), reproducible with the added `scripts/dep_stats.py`, plus build timings and incremental behaviour; the axiom audit (all listed endpoints report exactly `propext, Classical.choice, Quot.sound`); the two linter failures encountered and repaired (no compilation error occurred in this pass); the `InternalProjection` requirement table against the cleaned cover; and the note that the concrete legacy Task-23 projection under `Spine.E2.Model` is temporary and not the intended final Spin projection. `ARCHITECTURE.md`, `DEPENDENCY_DAG.md`, `REFACTOR_AUDIT.md` and `README.md` were updated accordingly; the historical trees are untouched.

**Still missing to instantiate the generic lift machinery** (stated exactly, nothing new proved): a topology and topological-ring structure on `Cl₃(ℝ)` (finite-dimensionality is packaging — the monomial basis is already in the intrinsic core, so it need not pass through M₂(ℂ)); the induced topological-group structures on `SpinGroup` and on the target Lorentz group; continuity of `spinCover`; discreteness of the two-element kernel (immediate from a T1 source topology and `Nat.card ker = 2`); and — the only genuinely missing mathematics — continuous local sections of `spinCover` around every element of the Lorentz group.

No `sorry`, `admit`, new axiom or `@[implemented_by]` anywhere; all work is committed and pushed.

# Summary of changes for run be3c44e5-1ec0-47b3-8ff0-04ab43fe430e
The refactoring task is complete and the whole tree builds green.

**Final verification.** A full `lake build` finishes with "Build completed successfully (8345 jobs)" and zero errors, including the comparison endpoints and the negative-control modules (the library glob covers every module, so the control builds are part of the same green build). The mechanical import firewall reports `SPINE FIREWALL AUDIT: all checks passed`. A repository-wide search confirms no `sorry`, no `admit`, no `axiom` declaration, no `@[implemented_by]` and no `native_decide`; every principal endpoint carries a `#print axioms` line and all of them report only `propext`, `Classical.choice`, `Quot.sound`.

**Active spine.**
- Experiment-1 half (`RequestProject/Spine/E1/*`, namespace `SpinCore`): the intrinsic real spin-factor carrier is now `SpinCore.LorentzCarrier`; the old carrier name is not exposed anywhere in the new API and the firewall checks that it does not occur in any production endpoint's proof closure. The legacy import coupling through the frozen-task module is gone — the carrier root imports Mathlib alone, and the two facts that upstream came through the Hermitian model are re-proved intrinsically. Preserved and proved: the intrinsic Lorentz quadratic form, the Lorentz Clifford construction, the intrinsic spin group, the surjective Spin→Lorentz action with kernel exactly `{±1}`, and the isomorphism with SL(2,ℂ). The Hermitian route is now only the comparison endpoint `RequestProject/Comparison/E1Hermitian.lean`. The algebraic Clifford-module / Weyl-spinor material was extracted into `Spine/E1/WeylSpinorModule.lean` with no Maxwell, gauge-field, quantization, mass-selection or positive-frequency dependency, and no historical verdicts carried forward.
- Experiment-2 half (`RequestProject/Spine/E2/*`): the serial chain is broken — the varying-family/frame/transition core no longer routes through the one-fibre comparison endpoint (now in `RequestProject/Comparison/`), the reusable local-lift machinery is a generic central-double-cover/transition-lift layer depending on Mathlib alone, and the later topology/lift/defect modules depend on that smallest core. The presentation-level vs family-level defect-neutrality distinction is preserved: family-level classification is proved in production without the good/bad atlas, which survives only as a negative control that no production module imports. The rank-three oriented-family defect is explicitly *not* identified with w₂, and neither w₁ nor w₂ is constructed.

**Documentation and audits.** `PROVENANCE_LEDGER.md` records every removed module (195 in total) with upstream experiment, original path, source revision, classification, strongest established result, and the reason for exclusion. `ARCHITECTURE.md` describes the layering and the two-level import firewall (generic production layer versus the model-instantiation layer that is permitted one deliberate historical import). `DEPENDENCY_DAG.md` gives before/after statistics and the exact transitive closure of each principal endpoint (e.g. files 488→319; the SL(2,ℂ) chain 48→19; the family core 202→7). `REFACTOR_AUDIT.md` records every failed build encountered during the refactor with its diagnosis and repair. `ARISTOTLE_SUMMARY.md` was read but not edited.

This session completed the remaining items: the architecture documents now describe the model-instantiation layer and the two-level firewall, the affected modules and the combined smoke test were rebuilt green, a full clean build was re-verified, and all work is committed and pushed. Nothing beyond the stopping condition was attempted.

# Summary of changes for run 09f6ba1f-47f4-4bd6-af6d-a1e0cb401233
The experiment naming has been normalized; the task is complete and the whole project builds green.

**Renaming performed (packaging only)**
- `RequestProject/Exp2/` → `RequestProject/Experiment1/` — **Intrinsic Branching from a Derived Lorentz Structure** (152 Lean files).
- `RequestProject/Exp1/` → `RequestProject/Experiment2/` — **Split Complex Lorentzian Dynamics** (334 Lean files).
- Directory moves were done with `git mv` (rename provenance is in the git history), and every reference was rewritten in one simultaneous pass: `RequestProject.Exp2 → RequestProject.Experiment1` (352 occurrences) and `RequestProject.Exp1 → RequestProject.Experiment2` (470 occurrences). A pre-check confirmed all `Exp1`/`Exp2` tokens were fully qualified module prefixes, so the rewrite is exactly a module-path transformation. No `Exp1`/`Exp2` aliases were introduced or retained: 0 occurrences remain in the Lean sources.

**Provenance audit (mechanical)**
A snapshot of the pre-rename trees was transformed by the prefix substitution alone and compared recursively with the post-rename trees: `E1 non-path source differences = 0`, `E2 non-path source differences = 0`. No statement, definition, proof, assumption, option setting or import of an external library was changed, and no namespace qualification beyond import-path rewriting was needed.

**Collision audit** — module paths: `NONE`; fully qualified declaration names: `NONE` (3270 E1 vs 6192 E2 declaration heads, empty genuine intersection; three apparent hits were prose words inside doc comments); one shared top-level namespace head `Counterexample` with disjoint leaf names, classified `RESOLVED_BY_MODULE_QUALIFICATION`. No `REQUIRES_DECLARATION_NAMESPACE_REFACTOR` case arose, so no declaration namespace was altered.

**Smoke test** — `RequestProject/CombinedDemo.lean` now imports `RequestProject.Experiment1.SpinFinal` and `RequestProject.Experiment2.NullSectorTask01.Task01` (the same representative endpoints as before, with labels corrected) and forwards one already-proved result from each (`CombinedDemo.experiment1_spin_double_cover`, `CombinedDemo.experiment2_task01_restriction`). No cross-experiment mathematics was added. Both correctly named experiments coexist and import simultaneously in one Lean environment; `#print axioms` on both endpoints reports only `propext`, `Classical.choice`, `Quot.sound`.

**Builds** — `lake build RequestProject.CombinedDemo`: `Build completed successfully (8043 jobs)`; `lake build`: `Build completed successfully (8514 jobs)`, 0 errors. One build failure occurred and was repaired (a `/-- … -/` doc comment placed before a `#print` command in the smoke-test file, changed to a line comment); it is recorded in the audit with error, diagnosis, repair, and confirmation that no mathematical content changed. Remaining output is 236 pre-existing upstream style-linter warnings plus the developments' own axiom-audit info messages. No `sorry`, `admit`, custom axioms, `implemented_by`, `native_decide`, `bv_decide`, or new `unsafe`/`partial`.

**Documentation** — added `NAMING_NORMALIZATION_AUDIT.md` (old/corrected mappings, file counts, exact prefix transformations and commands, difference counts, collision audit, focused and full build results with job counts, warnings, failed build and repair, acceptance-criteria table) and `COMBINED_PROJECT_PROVENANCE.md` (canonical names, source origins and branches, the recorded inversion and its correction, statement that no E1/E2 bridge exists). `README.md` was updated to the canonical terminology and explicitly notes that the previous archive reversed the `Exp1`/`Exp2` labels — described throughout as a packaging/provenance correction, not a mathematical one. `ARISTOTLE_SUMMARY.md` was left untouched as requested.

Stopping here as instructed: no cross-experiment mathematical bridge was begun.

# Summary of changes for run 9b1379ee-de0e-4d3b-ba3c-e4c62fab6b5e
Yes — I can pull in material from other repositories, as long as they are reachable (public git URLs work), and I've now combined both of the ones you gave me into this project.

What I found: neither repository is itself a Lake package at top level — each one ships its Lean development inside an archive containing a Lake project whose library is *also* called `RequestProject`. Because of that name clash, they can't simply be added as Lake dependencies (`require`) side by side. So instead I vendored both Lean sources into this project under distinct module prefixes:

- `RequestProject/Exp1/` — Experiment 1 (Split-Complex Lorentz Dynamics, `main`): 334 Lean files, `NullSectorTask01` … `NullSectorTask32`.
- `RequestProject/Exp2/` — Experiment 2 (Intrinsic Branching from a Derived Lorentz Structure branch): 152 Lean files, `Task10` … `Task32`, `SpinFinal`, `Herm2`, `Minkowski4`, etc.

All internal references `RequestProject.X` were rewritten to `RequestProject.Exp1.X` / `RequestProject.Exp2.X` (imports and doc comments). No statement, proof, definition or option setting was otherwise changed. Both bundles pin exactly the same toolchain and the same Mathlib revision as this project, so no dependency reconciliation was needed.

Verification:
- `lake build` completes successfully over all 486 vendored files plus the new demo module ("Build completed successfully (8514 jobs)"), with zero errors.
- A search confirms no `sorry`/`admit` anywhere in the sources, and there are no `axiom` declarations or `@[implemented_by]` attributes. The axiom audits emitted by the developments themselves report only `propext`, `Classical.choice`, `Quot.sound`.

I also added `RequestProject/CombinedDemo.lean`, which imports one module from each experiment into a single file and forwards one result from each (`CombinedDemo.exp1_task01_restriction` and `CombinedDemo.exp2_spin_double_cover`). It contains no new mathematics — it is a compiled witness that the two developments coexist and are usable together. Note that since the two were written independently they share some declaration namespaces (e.g. `Task10`, `Lorentz`), so a file importing from both may need fully qualified names in places.

`README.md` documents the origin of each subtree, the exact transformation applied, the build status, and how to import from either experiment.